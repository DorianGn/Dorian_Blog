# 在线学习平台功能清单

## 项目概述

**项目名称：** 在线学习平台（Online Learning Platform）
**技术架构：** .NET 5 + Ant Design Vue
**现有基础：** Colder.Admin.AntdVue 快速开发框架
**数据库：** MySQL（支持 SQL Server/PostgreSQL/Oracle）

---

## 一、系统角色定义

### 基于现有框架的角色体系（Base_Role）

| 角色 | 权限范围 | 对应现有角色 | 说明 |
|-----|---------|------------|------|
| **超级管理员** | 系统全局管理 | Admin（现有） | 系统配置、用户管理、数据审计 |
| **平台管理员** | 平台运营管理 | 新增角色 | 课程审核、讲师管理、数据统计 |
| **机构管理员** | 机构内管理 | 基于部门管理员扩展 | 机构课程、讲师、学员管理 |
| **讲师** | 课程管理 | 新增角色 | 创建课程、编辑内容、查看数据 |
| **学员** | 学习功能 | 新增角色（默认） | 选课、学习、作业、考试、证书 |
| **游客** | 浏览功能 | 无需登录 | 课程浏览、试听、注册 |

### 角色在 Base_User 中的体现
```csharp
// 扩展 Base_User.UserType 字段
public enum UserType
{
    Student = 0,      // 学员（默认）
    Teacher = 1,      // 讲师
    Admin = 2,        // 管理员
    InstitutionAdmin = 3  // 机构管理员
}
```

---

## 二、功能模块清单

### 1. 基础管理模块（Base_Manage）- 已有基础

#### 1.1 用户管理（扩展）
**基于现有：** Base_User

**扩展字段：**
```csharp
public class Base_User
{
    // 现有字段保留
    public string Id { get; set; }
    public string UserName { get; set; }
    public string Password { get; set; }
    public string RealName { get; set; }
    public int Sex { get; set; }
    public DateTime? Birthday { get; set; }
    public string DepartmentId { get; set; }  // 改为机构ID

    // 新增字段
    public string Avatar { get; set; }         // 头像URL
    public string Email { get; set; }          // 邮箱
    public string Phone { get; set; }          // 手机号
    public string Intro { get; set; }          // 个人简介
    public string Position { get; set; }       // 职位/职称
    public int UserType { get; set; }          // 用户类型：0=学员，1=讲师，2=管理员
    public DateTime? LastLoginTime { get; set; } // 最后登录时间
    public string LastLoginIp { get; set; }    // 最后登录IP
    public int Status { get; set; }            // 状态：0=正常，1=禁用
}
```

**新增功能：**
- 学员注册（需邮箱/手机验证）
- 讲师申请与审核
- 实名认证
- 第三方登录（微信、QQ、GitHub）
- 个人资料编辑
- 学习档案查看

**API接口：**
```
POST /Base_Manage/Base_User/StudentRegister        - 学员注册
POST /Base_Manage/Base_User/TeacherApply           - 讲师申请
POST /Base_Manage/Base_User/AuditTeacher            - 审核讲师
POST /Base_Manage/Base_User/UpdateProfile           - 更新资料
GET  /Base_Manage/Base_User/GetLearningProfile      - 学习档案
```

#### 1.2 机构管理（直接使用 Base_Department）
**改造方式：** 扩展 Base_Department 字段，无需新建表

**扩展字段（通过迁移添加）：**
```csharp
// 在现有 Base_Department 基础上扩展
public class Base_Department
{
    // === 现有字段保留 ===
    public string Id { get; set; }
    public string Name { get; set; }              // 机构/部门名称
    public string ParentId { get; set; }          // 父级ID（支持树形）
    public DateTime CreateTime { get; set; }
    public string CreatorId { get; set; }
    public bool Deleted { get; set; }

    // === 新增字段（用于机构扩展）===
    public string Logo { get; set; }              // 机构Logo
    public string Description { get; set; }       // 机构简介
    public string ContactPerson { get; set; }     // 联系人
    public string ContactPhone { get; set; }      // 联系电话
    public string Address { get; set; }           // 地址
    public int Type { get; set; }                 // 类型：0=普通部门，1=企业，2=高校，3=培训机构
    public string Certificate { get; set; }       // 资质证书URL
    public int Status { get; set; }               // 状态：0=正常，1=待审核，2=禁用
}
```

**优势：**
- 直接复用现有的树形结构逻辑（TreeHelper）
- 保持与现有权限系统的兼容性
- 用户表中的 DepartmentId 字段直接关联

**功能：**
- 机构入驻申请
- 机构信息管理
- 机构认证审核
- 子机构/分支管理
- 机构统计数据

**API接口（复用 Base_Department 接口）：**
```
POST /Base_Manage/Base_Department/SaveData         - 保存机构（复用现有）
POST /Base_Manage/Base_Department/GetDataList      - 机构列表（复用现有）
POST /Base_Manage/Base_Department/GetTreeDataList  - 机构树（复用现有）
POST /Base_Manage/Base_Department/AuditInstitution - 机构审核（新增）
GET  /Base_Manage/Base_Department/GetStatistics    - 机构统计（新增）
```

**利用现有框架能力：**
- ✅ 树形结构：TreeHelper 自动处理
- ✅ 权限控制：Base_Action 配置权限点
- ✅ 操作日志：AOP 自动记录
- ✅ 数据验证：ValidFilterAttribute
- ✅ 软删除：框架内置支持

---

### 2. 课程管理模块（Course_Manage）- 核心模块

#### 2.1 课程分类管理（Course_Category）

**表结构：**
```csharp
public class Course_Category
{
    public string Id { get; set; }
    public string Name { get; set; }              // 分类名称
    public string ParentId { get; set; }          // 父分类ID（支持多级）
    public string Icon { get; set; }              // 分类图标
    public string Cover { get; set; }             // 分类封面
    public string Description { get; set; }       // 描述
    public int Sort { get; set; }                 // 排序
    public bool IsRecommend { get; set; }         // 是否推荐
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 树形分类管理（如：编程 → Web开发 → Vue.js）
- 分类排序
- 推荐分类标记
- 分类课程统计

**API接口：**
```
POST /Course_Manage/Course_Category/GetTree        - 获取分类树
POST /Course_Manage/Course_Category/SaveData       - 保存分类
POST /Course_Manage/Course_Category/DeleteData     - 删除分类
GET  /Course_Manage/Course_Category/GetRecommend   - 推荐分类
```

#### 2.2 课程基本信息（Course）

**表结构：**
```csharp
public class Course
{
    public string Id { get; set; }
    public string Title { get; set; }             // 课程标题
    public string SubTitle { get; set; }          // 副标题
    public string Cover { get; set; }             // 封面图
    public string CategoryId { get; set; }        // 分类ID
    public string InstitutionId { get; set; }     // 所属机构ID
    public string TeacherId { get; set; }         // 主讲教师ID
    public string Description { get; set; }       // 课程描述
    public string Objectives { get; set; }        // 学习目标（JSON数组）
    public string Requirements { get; set; }      // 前置要求（JSON数组）
    public string Tags { get; set; }              // 标签（逗号分隔）

    // 课程属性
    public int CourseType { get; set; }           // 类型：0=录播视频，1=音频课程，2=图文课程
    public int Level { get; set; }                // 难度：0=入门，1=初级，2=中级，3=高级
    public int Duration { get; set; }             // 总时长（分钟）
    public int ChapterCount { get; set; }         // 章节数
    public int LessonCount { get; set; }          // 课时数

    // 价格与优惠
    public decimal OriginalPrice { get; set; }    // 原价
    public decimal CurrentPrice { get; set; }     // 现价
    public bool IsFree { get; set; }              // 是否免费
    public DateTime? DiscountStartTime { get; set; } // 优惠开始
    public DateTime? DiscountEndTime { get; set; }   // 优惠结束

    // 状态与统计
    public int Status { get; set; }               // 状态：0=草稿，1=待审核，2=已发布，3=下架
    public int StudentCount { get; set; }         // 学员数
    public int ViewCount { get; set; }            // 浏览数
    public decimal Rating { get; set; }           // 评分（0-5）
    public int ReviewCount { get; set; }          // 评价数

    // 推荐与排序
    public bool IsRecommend { get; set; }         // 是否推荐
    public bool IsHot { get; set; }               // 是否热门
    public bool IsNew { get; set; }               // 是否新课
    public int Sort { get; set; }                 // 排序

    // 审核信息
    public string AuditUserId { get; set; }       // 审核人ID
    public DateTime? AuditTime { get; set; }      // 审核时间
    public string AuditRemark { get; set; }       // 审核备注

    public DateTime CreateTime { get; set; }
    public string CreatorId { get; set; }
    public DateTime? PublishTime { get; set; }    // 发布时间
    public bool Deleted { get; set; }
}
```

**功能：**
- 课程创建与编辑
- 草稿保存
- 提交审核
- 课程上架/下架
- 课程复制
- 批量操作
- 课程预览

**API接口：**
```
POST /Course_Manage/Course/GetDataList             - 课程列表（分页、筛选）
POST /Course_Manage/Course/GetTheData              - 课程详情
POST /Course_Manage/Course/SaveData                - 保存课程
POST /Course_Manage/Course/SubmitAudit             - 提交审核
POST /Course_Manage/Course/AuditCourse             - 审核课程
POST /Course_Manage/Course/Publish                 - 发布课程
POST /Course_Manage/Course/Unpublish               - 下架课程
POST /Course_Manage/Course/CopyCourse              - 复制课程
GET  /Course_Manage/Course/GetRecommend            - 推荐课程
GET  /Course_Manage/Course/GetHot                  - 热门课程
GET  /Course_Manage/Course/GetStatistics           - 课程统计
```

#### 2.3 章节管理（Course_Chapter）

**表结构：**
```csharp
public class Course_Chapter
{
    public string Id { get; set; }
    public string CourseId { get; set; }          // 所属课程ID
    public string ParentId { get; set; }          // 父章节ID（支持子章节）
    public string Title { get; set; }             // 章节标题
    public string Description { get; set; }       // 章节描述
    public int Sort { get; set; }                 // 排序
    public bool IsFree { get; set; }              // 是否免费试学
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 多级章节管理
- 章节拖拽排序
- 批量添加章节
- 免费试学设置

**API接口：**
```
POST /Course_Manage/Course_Chapter/GetTree         - 获取章节树
POST /Course_Manage/Course_Chapter/SaveData        - 保存章节
POST /Course_Manage/Course_Chapter/UpdateSort      - 更新排序
POST /Course_Manage/Course_Chapter/DeleteData      - 删除章节
```

#### 2.4 课时管理（Course_Lesson）

**表结构：**
```csharp
public class Course_Lesson
{
    public string Id { get; set; }
    public string CourseId { get; set; }          // 所属课程ID
    public string ChapterId { get; set; }         // 所属章节ID
    public string Title { get; set; }             // 课时标题
    public string Description { get; set; }       // 课时描述
    public int Sort { get; set; }                 // 排序

    // 内容类型
    public int ContentType { get; set; }          // 类型：0=视频，1=音频，2=文档，3=图文
    public string VideoUrl { get; set; }          // 视频URL
    public string AudioUrl { get; set; }          // 音频URL
    public string DocumentUrl { get; set; }       // 文档URL（PDF、PPT等）
    public string Content { get; set; }           // 图文内容（富文本，使用WangEditor）

    // 媒体属性
    public int Duration { get; set; }             // 时长（秒）
    public string VideoId { get; set; }           // 视频ID（第三方）
    public string VideoCover { get; set; }        // 视频封面
    public int VideoQuality { get; set; }         // 清晰度：0=标清，1=高清，2=超清

    // 学习控制
    public bool IsFree { get; set; }              // 是否免费
    public bool IsRequired { get; set; }          // 是否必修
    public bool AllowDownload { get; set; }       // 允许下载
    public bool AllowDrag { get; set; }           // 允许拖拽进度
    public int PassScore { get; set; }            // 及格分（配合测验）

    // 资源附件
    public string Attachments { get; set; }       // 附件JSON（名称、URL、大小）

    // 统计
    public int ViewCount { get; set; }            // 观看数
    public int FinishCount { get; set; }          // 完成数

    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 多类型课时（视频、音频、文档、图文）
- 视频上传与转码
- 第三方视频集成（阿里云、腾讯云）
- 课时拖拽排序
- 附件管理（利用现有上传组件）
- 免费试听设置
- 课时预览

**利用现有框架能力：**
- ✅ 文件上传：CUploadFile、CUploadImg 组件
- ✅ 富文本编辑：WangEditor 组件
- ✅ 排序功能：TreeHelper 支持
- ✅ CRUD 生成：BuildCode 代码生成器

**API接口：**
```
POST /Course_Manage/Course_Lesson/GetList          - 课时列表
POST /Course_Manage/Course_Lesson/SaveData         - 保存课时
POST /Course_Manage/Course_Lesson/DeleteData       - 删除课时
POST /Course_Manage/Course_Lesson/UploadVideo      - 上传视频
POST /Course_Manage/Course_Lesson/GetVideoProgress - 视频转码进度
POST /Course_Manage/Course_Lesson/AddAttachment    - 添加附件
```

#### 2.5 教师管理（直接使用 Base_User，无需新表）

**设计说明：**
教师就是 `UserType=1` 的用户，无需单独建表。在 Base_User 基础上扩展字段即可。

**Base_User 扩展字段（针对讲师）：**
```csharp
// 在 Base_User 中新增以下字段
public class Base_User
{
    // === 现有字段省略 ===

    // === 讲师专用扩展字段 ===
    public string Specialty { get; set; }         // 擅长领域（讲师）
    public string Introduction { get; set; }      // 个人简介（富文本，讲师）
    public string Experience { get; set; }        // 工作经历（JSON，讲师）
    public string TeacherCertificates { get; set; } // 资质证书（JSON，讲师）

    // === 统计字段（通过查询计算，可选存储）===
    public int CourseCount { get; set; }          // 课程数（讲师）
    public int StudentCount { get; set; }         // 学员数（讲师）
    public decimal TeacherRating { get; set; }    // 评分（讲师）
    public int ReviewCount { get; set; }          // 评价数（讲师）

    // === 推荐标记 ===
    public bool IsRecommend { get; set; }         // 是否推荐（讲师/课程）
    public int Sort { get; set; }                 // 排序（用于推荐排序）
}
```

**功能：**
- 教师资料管理（扩展 Base_User 接口）
- 教师认证审核（UserType 转换：学员→讲师）
- 教师课程列表（根据 CreatorId 查询）
- 教师学员管理（通过课程关联）
- 教师数据统计（聚合查询）
- 推荐教师（IsRecommend=true）

**API接口（复用 Base_User 接口）：**
```
POST /Base_Manage/Base_User/GetDataList            - 用户列表（筛选 UserType=1）
POST /Base_Manage/Base_User/ApplyTeacher           - 申请成为讲师（新增）
POST /Base_Manage/Base_User/AuditTeacher           - 审核讲师（新增）
POST /Base_Manage/Base_User/GetTeacherProfile      - 获取讲师资料（新增）
POST /Base_Manage/Base_User/UpdateTeacherProfile   - 更新讲师资料（新增）
GET  /Base_Manage/Base_User/GetRecommendTeachers   - 推荐讲师（新增）
GET  /Base_Manage/Base_User/GetTeacherStatistics   - 讲师统计（新增）
```

**优势：**
- ✅ 统一用户管理，避免数据冗余
- ✅ 复用现有的 Base_UserBusiness、Base_UserCache
- ✅ 利用现有的权限体系（Base_Role + Base_Action）
- ✅ 利用现有的 AOP 日志记录
- ✅ 一个用户可以既是学员又是讲师（多角色）

---

### 3. 学习管理模块（Learning_Manage）

#### 3.1 我的课程（Learning_Course）

**表结构：**
```csharp
public class Learning_Course
{
    public string Id { get; set; }
    public string StudentId { get; set; }         // 学员ID
    public string CourseId { get; set; }          // 课程ID

    // 学习进度
    public int LearnedLessons { get; set; }       // 已学课时数
    public int TotalLessons { get; set; }         // 总课时数
    public int Progress { get; set; }             // 进度百分比
    public int LearnDuration { get; set; }        // 学习时长（分钟）

    // 学习状态
    public int Status { get; set; }               // 状态：0=学习中，1=已完成，2=已过期
    public DateTime? StartTime { get; set; }      // 开始学习时间
    public DateTime? FinishTime { get; set; }     // 完成时间
    public DateTime? LastLearnTime { get; set; }  // 最后学习时间
    public string LastLessonId { get; set; }      // 最后学习课时ID

    // 证书与成绩
    public bool HasCertificate { get; set; }      // 是否获得证书
    public string CertificateId { get; set; }     // 证书ID
    public decimal Score { get; set; }            // 综合得分

    // 有效期
    public DateTime? ExpireTime { get; set; }     // 过期时间
    public bool IsExpired { get; set; }           // 是否过期

    public DateTime CreateTime { get; set; }      // 加入时间
    public bool Deleted { get; set; }
}
```

**功能：**
- 我的课程列表
- 学习进度展示
- 继续学习
- 课程收藏
- 课程评价
- 学习证书查看

**API接口：**
```
GET  /Learning_Manage/Learning_Course/GetMyCourses     - 我的课程
POST /Learning_Manage/Learning_Course/JoinCourse       - 加入课程
GET  /Learning_Manage/Learning_Course/GetProgress      - 学习进度
GET  /Learning_Manage/Learning_Course/GetStatistics    - 学习统计
```

#### 3.2 学习记录（Learning_Record）

**表结构：**
```csharp
public class Learning_Record
{
    public string Id { get; set; }
    public string StudentId { get; set; }         // 学员ID
    public string CourseId { get; set; }          // 课程ID
    public string LessonId { get; set; }          // 课时ID

    // 学习详情
    public int LearnDuration { get; set; }        // 学习时长（秒）
    public int VideoProgress { get; set; }        // 视频进度（秒）
    public bool IsFinished { get; set; }          // 是否完成
    public DateTime? FinishTime { get; set; }     // 完成时间

    // 笔记与书签
    public string Notes { get; set; }             // 笔记内容
    public int BookmarkPosition { get; set; }     // 书签位置（秒）

    public DateTime CreateTime { get; set; }      // 记录时间
    public DateTime UpdateTime { get; set; }      // 更新时间
}
```

**功能：**
- 学习记录追踪
- 视频进度保存
- 断点续播
- 学习时长统计
- 学习报告生成

**API接口：**
```
POST /Learning_Manage/Learning_Record/SaveProgress     - 保存学习进度
GET  /Learning_Manage/Learning_Record/GetRecord        - 获取学习记录
POST /Learning_Manage/Learning_Record/MarkFinished     - 标记完成
GET  /Learning_Manage/Learning_Record/GetReport        - 学习报告
```

#### 3.3 笔记管理（Learning_Note）

**表结构：**
```csharp
public class Learning_Note
{
    public string Id { get; set; }
    public string StudentId { get; set; }         // 学员ID
    public string CourseId { get; set; }          // 课程ID
    public string LessonId { get; set; }          // 课时ID
    public int VideoTime { get; set; }            // 视频时间点（秒）
    public string Content { get; set; }           // 笔记内容（支持Markdown）
    public string Images { get; set; }            // 图片（JSON数组）
    public bool IsPublic { get; set; }            // 是否公开
    public int LikeCount { get; set; }            // 点赞数
    public DateTime CreateTime { get; set; }
    public DateTime UpdateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 视频时间点笔记
- Markdown编辑器
- 图片上传
- 笔记分享
- 笔记搜索
- 笔记导出

**API接口：**
```
POST /Learning_Manage/Learning_Note/GetList            - 笔记列表
POST /Learning_Manage/Learning_Note/SaveData           - 保存笔记
POST /Learning_Manage/Learning_Note/DeleteData         - 删除笔记
GET  /Learning_Manage/Learning_Note/GetPublicNotes     - 公开笔记
POST /Learning_Manage/Learning_Note/ExportNotes        - 导出笔记
```

#### 3.4 问答互动（Learning_Question）

**表结构：**
```csharp
public class Learning_Question
{
    public string Id { get; set; }
    public string StudentId { get; set; }         // 提问学员ID
    public string CourseId { get; set; }          // 课程ID
    public string LessonId { get; set; }          // 课时ID
    public int VideoTime { get; set; }            // 视频时间点

    public string Title { get; set; }             // 问题标题
    public string Content { get; set; }           // 问题内容（富文本）
    public string Images { get; set; }            // 图片（JSON）
    public string Tags { get; set; }              // 标签

    // 状态
    public int Status { get; set; }               // 状态：0=待回答，1=已回答，2=已关闭
    public bool IsSolved { get; set; }            // 是否已解决
    public string BestAnswerId { get; set; }      // 最佳答案ID

    // 统计
    public int AnswerCount { get; set; }          // 回答数
    public int ViewCount { get; set; }            // 浏览数
    public int LikeCount { get; set; }            // 点赞数

    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}

public class Learning_Answer
{
    public string Id { get; set; }
    public string QuestionId { get; set; }        // 问题ID
    public string UserId { get; set; }            // 回答者ID（讲师/学员）
    public string ParentId { get; set; }          // 父回答ID（支持回复）
    public string Content { get; set; }           // 回答内容（富文本）
    public string Images { get; set; }            // 图片
    public bool IsBest { get; set; }              // 是否最佳答案
    public bool IsTeacher { get; set; }           // 是否讲师回答
    public int LikeCount { get; set; }            // 点赞数
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 课程问答
- 讲师答疑
- 学员互助
- 最佳答案
- 问答搜索
- 问答通知

**API接口：**
```
POST /Learning_Manage/Learning_Question/AskQuestion    - 提问
POST /Learning_Manage/Learning_Question/AnswerQuestion - 回答
POST /Learning_Manage/Learning_Question/SetBestAnswer  - 设置最佳答案
GET  /Learning_Manage/Learning_Question/GetQuestions   - 问题列表
```

---

### 4. 作业考试模块（Exam_Manage）

#### 4.1 题库管理（Exam_QuestionBank）

**表结构：**
```csharp
public class Exam_QuestionBank
{
    public string Id { get; set; }
    public string Name { get; set; }              // 题库名称
    public string CategoryId { get; set; }        // 分类ID
    public string InstitutionId { get; set; }     // 所属机构
    public string Description { get; set; }       // 描述
    public int QuestionCount { get; set; }        // 题目数量
    public bool IsPublic { get; set; }            // 是否公开
    public DateTime CreateTime { get; set; }
    public string CreatorId { get; set; }
    public bool Deleted { get; set; }
}
```

#### 4.2 试题管理（Exam_Question）

**表结构：**
```csharp
public class Exam_Question
{
    public string Id { get; set; }
    public string BankId { get; set; }            // 题库ID
    public int QuestionType { get; set; }         // 题型：0=单选，1=多选，2=判断，3=填空，4=简答，5=编程
    public string Title { get; set; }             // 题目标题（支持富文本）
    public string Content { get; set; }           // 题目内容
    public string Options { get; set; }           // 选项（JSON）
    public string Answer { get; set; }            // 正确答案
    public string Analysis { get; set; }          // 答案解析
    public int Difficulty { get; set; }           // 难度：0=简单，1=中等，2=困难
    public int Score { get; set; }                // 分值
    public string Tags { get; set; }              // 标签
    public int UseCount { get; set; }             // 使用次数
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 题库分类管理
- 试题增删改查
- 批量导入题目（Excel）
- 题目预览
- 随机组卷
- 题目统计分析

**API接口：**
```
POST /Exam_Manage/Exam_Question/GetList                - 题目列表
POST /Exam_Manage/Exam_Question/SaveData               - 保存题目
POST /Exam_Manage/Exam_Question/ImportQuestions        - 批量导入
POST /Exam_Manage/Exam_Question/RandomGenerate         - 随机组卷
```

#### 4.3 作业管理（Exam_Homework）

**表结构：**
```csharp
public class Exam_Homework
{
    public string Id { get; set; }
    public string CourseId { get; set; }          // 课程ID
    public string LessonId { get; set; }          // 课时ID
    public string Title { get; set; }             // 作业标题
    public string Description { get; set; }       // 作业说明
    public string Content { get; set; }           // 作业内容（富文本）
    public string Attachments { get; set; }       // 附件（JSON）

    // 配置
    public int HomeworkType { get; set; }         // 类型：0=客观题，1=主观题，2=编程题，3=项目作业
    public int TotalScore { get; set; }           // 总分
    public int PassScore { get; set; }            // 及格分
    public int TimeLimit { get; set; }            // 时限（分钟，0=不限制）
    public int MaxAttempts { get; set; }          // 最大提交次数

    // 时间
    public DateTime StartTime { get; set; }       // 开始时间
    public DateTime EndTime { get; set; }         // 截止时间
    public bool AllowLateSubmit { get; set; }     // 允许迟交

    // 统计
    public int SubmitCount { get; set; }          // 提交人数
    public int PassCount { get; set; }            // 通过人数
    public decimal AvgScore { get; set; }         // 平均分

    public DateTime CreateTime { get; set; }
    public string CreatorId { get; set; }
    public bool Deleted { get; set; }
}
```

#### 4.4 作业提交（Exam_HomeworkSubmit）

**表结构：**
```csharp
public class Exam_HomeworkSubmit
{
    public string Id { get; set; }
    public string HomeworkId { get; set; }        // 作业ID
    public string StudentId { get; set; }         // 学员ID
    public int AttemptCount { get; set; }         // 提交次数

    // 提交内容
    public string Answer { get; set; }            // 答案内容
    public string Attachments { get; set; }       // 附件（JSON）
    public string CodeUrl { get; set; }           // 代码仓库URL

    // 评分
    public int Status { get; set; }               // 状态：0=待批改，1=已批改，2=需修改
    public decimal Score { get; set; }            // 得分
    public bool IsPassed { get; set; }            // 是否通过
    public string Comment { get; set; }           // 教师评语
    public string TeacherId { get; set; }         // 批改教师ID
    public DateTime? ReviewTime { get; set; }     // 批改时间

    public DateTime SubmitTime { get; set; }      // 提交时间
    public bool IsLate { get; set; }              // 是否迟交
    public bool Deleted { get; set; }
}
```

**功能：**
- 作业发布
- 作业提交
- 作业批改
- 成绩统计
- 作业归档

**API接口：**
```
POST /Exam_Manage/Exam_Homework/Publish                - 发布作业
POST /Exam_Manage/Exam_Homework/Submit                 - 提交作业
POST /Exam_Manage/Exam_Homework/Review                 - 批改作业
GET  /Exam_Manage/Exam_Homework/GetSubmissions         - 提交列表
GET  /Exam_Manage/Exam_Homework/GetStatistics          - 作业统计
```

#### 4.5 考试管理（Exam_Paper）

**表结构：**
```csharp
public class Exam_Paper
{
    public string Id { get; set; }
    public string CourseId { get; set; }          // 课程ID
    public string Title { get; set; }             // 试卷标题
    public string Description { get; set; }       // 说明
    public int PaperType { get; set; }            // 类型：0=固定试卷，1=随机试卷

    // 试卷配置
    public string QuestionIds { get; set; }       // 题目ID列表（JSON）
    public int TotalScore { get; set; }           // 总分
    public int PassScore { get; set; }            // 及格分
    public int Duration { get; set; }             // 考试时长（分钟）

    // 规则
    public bool IsRandom { get; set; }            // 随机出题
    public bool AllowRetake { get; set; }         // 允许重考
    public int MaxAttempts { get; set; }          // 最大考试次数
    public bool ShowAnswer { get; set; }          // 显示答案
    public bool ShowAnalysis { get; set; }        // 显示解析

    // 时间
    public DateTime StartTime { get; set; }
    public DateTime EndTime { get; set; }

    // 统计
    public int ExamCount { get; set; }            // 参考人数
    public int PassCount { get; set; }            // 通过人数
    public decimal AvgScore { get; set; }         // 平均分
    public decimal HighestScore { get; set; }     // 最高分

    public DateTime CreateTime { get; set; }
    public string CreatorId { get; set; }
    public bool Deleted { get; set; }
}
```

#### 4.6 考试记录（Exam_Record）

**表结构：**
```csharp
public class Exam_Record
{
    public string Id { get; set; }
    public string PaperId { get; set; }           // 试卷ID
    public string StudentId { get; set; }         // 考生ID
    public int AttemptCount { get; set; }         // 考试次数

    // 考试过程
    public DateTime StartTime { get; set; }       // 开始时间
    public DateTime? EndTime { get; set; }        // 结束时间
    public int Duration { get; set; }             // 用时（秒）
    public int Status { get; set; }               // 状态：0=考试中，1=已提交，2=超时，3=中断

    // 答题内容
    public string Answers { get; set; }           // 答案（JSON）
    public string Questions { get; set; }         // 题目快照（JSON）

    // 成绩
    public decimal Score { get; set; }            // 得分
    public bool IsPassed { get; set; }            // 是否通过
    public string CorrectAnswers { get; set; }    // 正确答案（JSON）

    // 防作弊
    public int SwitchCount { get; set; }          // 切屏次数
    public string IpAddress { get; set; }         // IP地址

    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 考试发布
- 在线考试
- 自动评分
- 人工阅卷
- 成绩查询
- 考试分析
- 防作弊监控

**API接口：**
```
POST /Exam_Manage/Exam_Paper/Publish                   - 发布考试
POST /Exam_Manage/Exam_Paper/StartExam                 - 开始考试
POST /Exam_Manage/Exam_Paper/SubmitExam                - 提交试卷
GET  /Exam_Manage/Exam_Paper/GetResult                 - 查询成绩
GET  /Exam_Manage/Exam_Paper/GetAnalysis               - 考试分析
POST /Exam_Manage/Exam_Paper/ManualReview              - 人工阅卷
```

---

### 5. 评价系统模块（Review_Manage）

#### 5.1 课程评价（Review_Course）

**表结构：**
```csharp
public class Review_Course
{
    public string Id { get; set; }
    public string CourseId { get; set; }          // 课程ID
    public string StudentId { get; set; }         // 学员ID
    public decimal Rating { get; set; }           // 评分（1-5星）
    public string Content { get; set; }           // 评价内容
    public string Tags { get; set; }              // 标签（通俗易懂、内容丰富...）
    public bool IsAnonymous { get; set; }         // 是否匿名
    public int LikeCount { get; set; }            // 点赞数
    public int Status { get; set; }               // 状态：0=待审核，1=已发布，2=已屏蔽
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}

public class Review_Reply
{
    public string Id { get; set; }
    public string ReviewId { get; set; }          // 评价ID
    public string UserId { get; set; }            // 回复者ID
    public string Content { get; set; }           // 回复内容
    public bool IsTeacher { get; set; }           // 是否教师回复
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 课程评价
- 评价回复
- 评价点赞
- 评价举报
- 评价审核
- 评分统计

**API接口：**
```
POST /Review_Manage/Review_Course/Submit               - 提交评价
POST /Review_Manage/Review_Course/Reply                - 回复评价
POST /Review_Manage/Review_Course/Like                 - 点赞评价
GET  /Review_Manage/Review_Course/GetList              - 评价列表
GET  /Review_Manage/Review_Course/GetStatistics        - 评价统计
```

#### 5.2 教师评价（Review_Teacher）

**表结构：** 类似 Review_Course

**功能：**
- 教师评价
- 教学质量反馈
- 评分统计

---

### 6. 证书系统模块（Certificate_Manage）

#### 7.1 证书模板（Certificate_Template）

**表结构：**
```csharp
public class Certificate_Template
{
    public string Id { get; set; }
    public string Name { get; set; }              // 模板名称
    public string BackgroundUrl { get; set; }     // 背景图
    public string Layout { get; set; }            // 布局配置（JSON）
    public string Variables { get; set; }         // 变量（姓名、课程、日期...）
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

#### 7.2 学员证书（Certificate）

**表结构：**
```csharp
public class Certificate
{
    public string Id { get; set; }
    public string CertificateNo { get; set; }     // 证书编号
    public string StudentId { get; set; }         // 学员ID
    public string CourseId { get; set; }          // 课程ID
    public string TemplateId { get; set; }        // 模板ID
    public string CertificateUrl { get; set; }    // 证书图片URL
    public string PdfUrl { get; set; }            // 证书PDF URL
    public decimal Score { get; set; }            // 综合成绩
    public DateTime IssueDate { get; set; }       // 颁发日期
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 证书模板管理
- 自动生成证书
- 证书查询
- 证书下载
- 证书验证
- 证书分享

**API接口：**
```
POST /Certificate_Manage/Certificate/Generate          - 生成证书
GET  /Certificate_Manage/Certificate/GetMy             - 我的证书
GET  /Certificate_Manage/Certificate/Verify            - 验证证书
GET  /Certificate_Manage/Certificate/Download          - 下载证书
```

---

### 7. 消息通知模块（Message_Manage）

#### 8.1 系统消息（Message）

**表结构：**
```csharp
public class Message
{
    public string Id { get; set; }
    public int MessageType { get; set; }          // 类型：0=系统通知，1=课程通知，2=作业通知，3=考试通知，4=互动消息
    public string Title { get; set; }             // 标题
    public string Content { get; set; }           // 内容
    public string Link { get; set; }              // 链接
    public string SenderId { get; set; }          // 发送者ID
    public string ReceiverId { get; set; }        // 接收者ID（空=全体）
    public bool IsRead { get; set; }              // 是否已读
    public DateTime? ReadTime { get; set; }       // 阅读时间
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 系统通知
- 课程通知
- 作业提醒
- 考试提醒
- 问答回复通知
- 站内信
- 消息推送

**API接口：**
```
POST /Message_Manage/Message/Send                      - 发送消息
GET  /Message_Manage/Message/GetList                   - 消息列表
POST /Message_Manage/Message/MarkRead                  - 标记已读
GET  /Message_Manage/Message/GetUnreadCount            - 未读数
```

---

### 8. 数据统计模块（Statistics_Manage）

#### 9.1 平台统计

**功能：**
- 总体数据（用户数、课程数、订单数）
- 增长趋势
- 活跃度分析
- 营收统计
- 热门课程排行
- 热门讲师排行

**API接口：**
```
GET  /Statistics_Manage/Dashboard/GetOverview          - 概览数据
GET  /Statistics_Manage/Dashboard/GetTrend             - 趋势图
GET  /Statistics_Manage/Dashboard/GetRanking           - 排行榜
```

#### 9.2 课程统计

**功能：**
- 课程访问量
- 学员数统计
- 完课率
- 评分趋势
- 收入统计

**API接口：**
```
GET  /Statistics_Manage/Course/GetStatistics           - 课程统计
GET  /Statistics_Manage/Course/GetStudentAnalysis      - 学员分析
```

#### 9.3 学员统计

**功能：**
- 学习时长统计
- 学习进度
- 成绩分析
- 活跃度分析

**API接口：**
```
GET  /Statistics_Manage/Student/GetProfile             - 学习档案
GET  /Statistics_Manage/Student/GetAnalysis            - 学习分析
```

---

### 9. 系统配置模块（System_Manage）- 基于现有框架

#### 11.1 系统设置（基于现有配置机制）

**利用现有框架：**
- ✅ appsettings.json 配置文件
- ✅ IConfiguration 配置读取
- ✅ 环境变量配置

**配置内容：**
- 网站基本信息（名称、Logo、Favicon）
- 第三方配置：
  - 云存储配置（OSS、COS）
  - 短信配置（阿里云、腾讯云）
  - 邮件配置（SMTP）
  - 视频云配置（阿里云MPS、腾讯云MPS）
- 安全配置（JWT密钥、加密配置）

#### 9.2 字典管理（新增表：System_Dictionary）

**表结构：**
```csharp
public class System_Dictionary
{
    public string Id { get; set; }
    public string Type { get; set; }              // 字典类型（CourseLevel、CourseType等）
    public string Code { get; set; }              // 编码
    public string Name { get; set; }              // 名称
    public int Value { get; set; }                // 值
    public int Sort { get; set; }                 // 排序
    public string Remark { get; set; }            // 备注
    public bool IsEnabled { get; set; }           // 是否启用
    public DateTime CreateTime { get; set; }
    public bool Deleted { get; set; }
}
```

**功能：**
- 枚举值可视化管理
- 动态配置选项
- 避免硬编码

**利用现有框架：**
- ✅ 使用 BaseBusiness<T> 快速实现 CRUD
- ✅ 使用 BuildCode 生成器生成代码

#### 9.3 操作日志（直接使用 Base_UserLog）

**现有表：** Base_UserLog

**现有功能：**
- ✅ AOP 自动记录操作日志（DataAddLog、DataEditLog、DataDeleteLog）
- ✅ 日志查询（Base_UserLogController）
- ✅ 操作者信息（IOperator）
- ✅ Serilog 日志框架

**扩展功能（新增）：**
- 登录日志查询
- 异常日志查询
- 日志导出
- 日志统计分析

---

## 三、数据库表设计

### 3.1 现有表复用（8张）

| 序号 | 表名 | 说明 | 扩展方式 |
|-----|------|------|---------|
| 1 | Base_User | 用户表 | **扩展字段**：Avatar, Email, Phone, Intro, Position, UserType, LastLoginTime, LastLoginIp, Status, Specialty, Introduction, Experience, TeacherCertificates, CourseCount, StudentCount, TeacherRating, ReviewCount, IsRecommend, Sort |
| 2 | Base_Role | 角色表 | **直接使用**：新增学员、讲师、机构管理员角色 |
| 3 | Base_Action | 权限/菜单表 | **扩展数据**：添加课程、订单、学习等权限点 |
| 4 | Base_UserRole | 用户角色关联 | **直接使用** |
| 5 | Base_RoleAction | 角色权限关联 | **直接使用** |
| 6 | Base_Department | 部门/机构表 | **扩展字段**：Logo, Description, ContactPerson, ContactPhone, Address, Type, Certificate, Status |
| 7 | Base_UserLog | 操作日志表 | **直接使用** + 扩展日志查询功能 |
| 8 | Base_AppSecret | 应用密钥表 | **直接使用** |

### 3.2 新增核心表（约27张）

**课程管理（5张）**
| 序号 | 表名 | 说明 |
|-----|------|------|
| 9 | Course_Category | 课程分类 |
| 10 | Course | 课程信息 |
| 11 | Course_Chapter | 章节 |
| 12 | Course_Lesson | 课时 |
| 13 | Course_Favorite | 收藏 |

**学习管理（5张）**
| 序号 | 表名 | 说明 |
|-----|------|------|
| 14 | Learning_Course | 我的课程 |
| 15 | Learning_Record | 学习记录 |
| 16 | Learning_Note | 笔记 |
| 17 | Learning_Question | 提问 |
| 18 | Learning_Answer | 回答 |

**作业考试（6张）**
| 序号 | 表名 | 说明 |
|-----|------|------|
| 19 | Exam_QuestionBank | 题库 |
| 20 | Exam_Question | 试题 |
| 21 | Exam_Homework | 作业 |
| 22 | Exam_HomeworkSubmit | 作业提交 |
| 23 | Exam_Paper | 试卷 |
| 24 | Exam_Record | 考试记录 |

**评价系统（3张）**
| 序号 | 表名 | 说明 |
|-----|------|------|
| 25 | Review_Course | 课程评价 |
| 26 | Review_Teacher | 教师评价 |
| 27 | Review_Reply | 评价回复 |

**证书系统（2张）**
| 序号 | 表名 | 说明 |
|-----|------|------|
| 28 | Certificate_Template | 证书模板 |
| 29 | Certificate | 证书 |

**消息通知（2张）**
| 序号 | 表名 | 说明 |
|-----|------|------|
| 30 | Message | 消息 |
| 31 | Message_Template | 消息模板 |

**系统配置（2张）**
| 序号 | 表名 | 说明 |
|-----|------|------|
| 32 | System_Dictionary | 数据字典 |
| 33 | System_File | 文件管理 |

**数据统计**
- 无需额外表，基于现有表通过聚合查询实现

### 3.3 数据库表总结

- **现有表复用**：8张（充分利用框架）
- **新增核心表**：27张（纯学习平台业务）
- **总计**：35张表
- **对比第一版方案**：减少了18张表（去掉直播3张 + 合并教师表 + 合并机构表 + 去掉订单支付4张 + 去掉内容管理4张）
- **设计理念**：聚焦核心学习功能，课程全部免费或通过其他系统处理支付

### 3.4 框架集成优势

| 框架能力 | 复用效果 |
|---------|---------|
| **BaseBusiness<T>** | 所有新表的 CRUD 自动继承 |
| **BuildCode 代码生成器** | 快速生成新模块代码 |
| **TreeHelper** | Course_Category、Base_Department 树形结构 |
| **AOP 日志** | 自动记录所有操作日志 |
| **权限系统** | Base_Action 扩展新功能权限点 |
| **缓存机制** | BaseCache<T> 扩展课程、学习缓存 |
| **软删除** | 所有表自动支持 Deleted 字段 |
| **雪花ID** | IdHelper 自动生成分布式ID |
| **EFCore Sharding** | 支持分库分表（大数据量场景）|

---

## 四、前端页面规划

### 管理后台（Admin）

**基础管理**
- 用户管理（学员、讲师）
- 机构管理
- 角色权限管理

**课程管理**
- 课程列表
- 课程编辑（多步骤表单）
- 章节课时管理
- 教师管理
- 课程审核
- 课程分类管理

**学习管理**
- 学员管理
- 学习记录
- 问答管理
- 笔记管理

**作业考试**
- 题库管理
- 试题编辑
- 作业管理
- 考试管理
- 成绩管理

**评价管理**
- 课程评价审核
- 教师评价管理

**消息管理**
- 消息通知
- 消息模板

**数据统计**
- 数据概览
- 课程统计
- 学员统计
- 学习行为分析

**系统设置**
- 系统配置
- 字典管理
- 操作日志

### 用户前台（Web）

**首页**
- 推荐课程
- 热门课程
- 名师推荐
- 课程分类导航

**课程中心**
- 课程分类
- 课程列表（筛选、排序）
- 课程详情
- 课程评价
- 讲师主页

**学习中心**
- 我的课程
- 学习记录
- 我的笔记
- 我的问答
- 我的作业
- 我的考试
- 我的证书

**个人中心**
- 个人信息
- 学习档案
- 收藏课程
- 消息通知
- 账号设置

**课程播放页**
- 视频播放器（DPlayer / Aliyun Player）
- 章节目录（可拖拽）
- 课程介绍（富文本展示）
- 课程笔记（Markdown支持）
- 课程问答（实时问答）
- 课程资料（下载附件）

---

## 五、技术实现要点与框架集成

### 1. 视频处理
- **存储方案：** 阿里云OSS / 腾讯云COS
- **视频转码：** 阿里云MPS / 腾讯云MPS
- **播放器：** 阿里云播放器 / DPlayer / Video.js
- **防盗链：** URL签名、时间戳
- **加密：** HLS加密
- **框架集成：**
  - ✅ 文件上传服务封装（IFileService）
  - ✅ 视频转码回调处理（Webhook Controller）
  - ✅ 播放地址签名生成（VideoHelper）

### 2. 支付集成
- **微信支付：** Native / JSAPI / H5
- **支付宝：** PC / 手机网站 / APP
- **回调处理：** 异步通知、订单状态同步

### 2. 缓存策略
- **Redis缓存：**
  - 用户信息缓存（复用 IBase_UserCache 模式）
  - 课程信息缓存（新增 ICourseCache）
  - 学习进度缓存（新增 ILearningCache）
  - 热门数据缓存（排行榜、推荐）
  - 验证码缓存
- **CDN加速：** 静态资源、视频
- **框架集成：**
  - ✅ BaseCache<T> 泛型缓存基类
  - ✅ IDistributedCache 分布式缓存
  - ✅ 缓存配置（appsettings.json）
  - ✅ 缓存过期策略

### 3. 搜索优化
- **全文搜索：** Elasticsearch（可选）或 数据库LIKE（MVP阶段）
- **搜索字段：** 课程标题、描述、标签、讲师
- **框架集成：**
  - ✅ LINQ 动态查询（WhereIf 扩展方法）
  - ✅ 分页查询（GetPageResultAsync）
  - ✅ 多条件筛选

### 4. 消息推送
- **站内信：** Message 表 + SignalR
- **邮件：** SMTP / SendCloud
- **短信：** 阿里云 / 腾讯云
- **APP推送：** 极光推送 / 个推
- **框架集成：**
  - ✅ 消息服务封装（IMessageService）
  - ✅ 邮件模板（Razor Template Engine）
  - ✅ SignalR Hub 配置
  - ✅ 定时任务（Hangfire 或 HostedService）

### 6. 数据统计
- **实时统计：** Redis计数器
- **报表生成：** 定时任务 + 数据库统计
- **图表展示：** ECharts

### 6. 防作弊
- **考试监控：** 切屏检测（前端JS）、时间限制、IP记录
- **视频防刷：** 观看进度校验、心跳检测（每30秒上报）
- **接口防刷：** 限流（AspNetCoreRateLimit）、验证码
- **框架集成：**
  - ✅ 请求限流中间件
  - ✅ 数据重复验证（DataRepeatValidateAttribute）
  - ✅ IP地址获取（HttpContext）
  - ✅ 异常日志记录（GlobalExceptionFilter）

### 7. 权限控制扩展

**现有权限系统：** Base_Role + Base_Action + Base_RoleAction

**学习平台权限点设计：**

```
课程管理
├─ Course.View          - 查看课程
├─ Course.Add           - 新增课程
├─ Course.Edit          - 编辑课程
├─ Course.Delete        - 删除课程
├─ Course.Audit         - 审核课程
├─ Course.Publish       - 发布课程
├─ Chapter.Manage       - 章节管理
└─ Lesson.Manage        - 课时管理

学员管理
├─ Student.View         - 查看学员
├─ Student.Edit         - 编辑学员
└─ Student.Disable      - 禁用学员

讲师管理
├─ Teacher.View         - 查看讲师
├─ Teacher.Audit        - 审核讲师
└─ Teacher.Disable      - 禁用讲师

学习管理
├─ Learning.View        - 查看学习记录
├─ Note.Manage          - 笔记管理
└─ Question.Manage      - 问答管理

作业考试
├─ Exam.Manage          - 考试管理
├─ Homework.Manage      - 作业管理
└─ Question.Manage      - 题库管理

数据统计
├─ Statistics.Platform  - 平台统计
├─ Statistics.Course    - 课程统计
└─ Statistics.Learning  - 学习统计
```

**实现方式：**
- ✅ 在 Base_Action 表中添加上述权限点
- ✅ 使用 ApiPermissionAttribute 标记控制器方法
- ✅ 利用现有的 PermissionBusiness 进行权限计算

**示例：**
```csharp
[ApiPermission("Course.Add")]
public async Task<AjaxResult> SaveData(CourseEditInputDTO input)
{
    // 业务逻辑
}
```

---

## 六、开发优先级与阶段规划

### 第一阶段：MVP 核心功能（3周）

**目标：** 实现最小可用产品，验证核心业务流程

| 模块 | 功能点 | 基于框架能力 | 预估工时 |
|-----|--------|------------|---------|
| **用户管理** | 学员注册、登录、个人资料 | Base_User扩展 + 现有登录 | 3人天 |
| **讲师申请** | 申请成为讲师、审核 | Base_User.UserType切换 | 2人天 |
| **机构管理** | 机构信息管理 | Base_Department扩展 | 2人天 |
| **课程管理** | 课程增删改查、分类 | BuildCode生成 + 手动优化 | 8人天 |
| **章节课时** | 章节课时管理、视频上传 | TreeHelper + 文件上传 | 5人天 |
| **课程浏览** | 课程列表、详情、搜索 | BaseBusiness分页查询 | 4人天 |
| **视频播放** | 播放器集成、进度记录 | 前端播放器 + API | 4人天 |

**小计：** 28人天（1后端 + 1前端 = 约3周）

**里程碑：**
- ✅ 用户可以注册、登录
- ✅ 讲师可以创建课程、上传视频
- ✅ 学员可以浏览课程、观看视频

---

### 第二阶段：学习体验完善（2周）

**目标：** 完善学习功能与互动体验

| 模块 | 功能点 | 基于框架能力 | 预估工时 |
|-----|--------|------------|---------|
| **我的课程** | 课程列表、学习进度 | BaseBusiness查询 | 3人天 |
| **笔记功能** | 笔记增删改查、导出 | BuildCode + Markdown | 3人天 |
| **问答互动** | 提问、回答、最佳答案 | BuildCode生成 | 4人天 |
| **评价系统** | 课程评价、讲师评价 | BuildCode生成 | 3人天 |
| **权限扩展** | 配置新权限点 | Base_Action数据维护 | 1人天 |

**小计：** 14人天（约2周）

**里程碑：**
- ✅ 学员可以加入课程学习
- ✅ 完整的学习体验（笔记、问答、评价）
- ✅ 讲师可以查看数据和回复问题

---

### 第三阶段：作业考试与证书（3周）

**目标：** 完善学习管理功能，提升用户体验

| 模块 | 功能点 | 基于框架能力 | 预估工时 |
|-----|--------|------------|---------|
| **题库管理** | 题库、试题增删改查 | BuildCode生成 | 3人天 |
| **作业系统** | 发布、提交、批改 | BuildCode + 文件上传 | 5人天 |
| **考试系统** | 组卷、考试、评分 | BuildCode + 业务逻辑 | 7人天 |
| **证书系统** | 证书模板、自动生成 | 图片处理库 | 4人天 |
| **消息通知** | 站内信、邮件提醒 | SignalR + SMTP | 4人天 |

**小计：** 23人天（约3周）

**里程碑：**
- ✅ 完整的作业考试功能
- ✅ 学员可以获得证书
- ✅ 消息及时触达

---

### 第四阶段：数据统计与优化（2周）

**目标：** 数据统计、系统优化、上线准备

| 模块 | 功能点 | 基于框架能力 | 预估工时 |
|-----|--------|------------|---------|
| **数据统计** | 平台、课程、学员统计 | LINQ聚合查询 + ECharts | 5人天 |
| **系统优化** | 缓存优化、性能调优 | Redis + 索引优化 | 4人天 |
| **测试完善** | 全链路测试、Bug修复 | 单元测试 + 集成测试 | 5人天 |

**小计：** 14人天（约2周）

**里程碑：**
- ✅ 完整的数据分析看板
- ✅ 系统稳定性提升
- ✅ 可上线运营

---

### 阶段总结

| 阶段 | 周期 | 工时 | 累计工时 |
|-----|------|------|---------|
| 第一阶段（MVP） | 3周 | 28人天 | 28人天 |
| 第二阶段（学习完善） | 2周 | 14人天 | 42人天 |
| 第三阶段（作业考试） | 3周 | 23人天 | 65人天 |
| 第四阶段（统计优化） | 2周 | 14人天 | 79人天 |

**总计：** 79人天（约10周）= **2-2.5个月**

**建议团队配置：**
- **方案一（推荐）：** 2名后端 + 2名前端 + 1名测试 = 实际交付 **约2个月**
- **方案二：** 1名后端 + 1名前端 + 1名测试（兼） = 实际交付 **约3-4个月**

---

## 七、现有框架深度集成方案

### 7.1 直接复用的核心能力

| 框架模块 | 复用方式 | 应用场景 |
|---------|---------|---------|
| **BaseBusiness<T>** | 继承泛型基类 | 所有新表的增删改查、分页查询 |
| **BuildCode** | 使用代码生成器 | 快速生成Course、Order等模块代码 |
| **Base_User** | 扩展字段 | 统一管理学员、讲师、管理员 |
| **Base_Role + Base_Action** | 扩展权限点 | 课程、订单、学习等权限控制 |
| **Base_Department** | 扩展字段 | 机构/部门树形管理 |
| **Base_UserLog** | 直接使用 | AOP自动记录操作日志 |
| **TreeHelper** | 工具类调用 | 课程分类、机构树、章节树 |
| **IdHelper** | 工具类调用 | 雪花ID生成 |
| **CUploadFile/CUploadImg** | 前端组件 | 视频、图片、附件上传 |
| **WangEditor** | 前端组件 | 课程描述、文章、问答内容编辑 |
| **IconSelector** | 前端组件 | 分类图标选择 |
| **BaseCache<T>** | 继承缓存基类 | 课程、用户、学习数据缓存 |

### 7.2 AOP 能力扩展应用

**现有 AOP 属性：**
```csharp
[DataAddLog]            // 自动记录新增日志
[DataEditLog]           // 自动记录编辑日志
[DataDeleteLog]         // 自动记录删除日志
[DataRepeatValidate]    // 防重复提交
```

**应用示例：**
```csharp
public class CourseBusiness : BaseBusiness<Course>, ICourseBusiness, ITransientDependency
{
    [DataAddLog("创建课程")]
    public async Task SaveDataAsync(CourseEditInputDTO input)
    {
        // 业务逻辑，无需手动写日志
        // AOP 会自动记录到 Base_UserLog
    }

    [DataEditLog("审核课程")]
    public async Task AuditCourseAsync(string courseId, bool isPass)
    {
        // 审核逻辑，自动记录审核日志
    }

    [DataRepeatValidate]
    public async Task SubmitOrderAsync(OrderInputDTO input)
    {
        // 防止重复下单
    }
}
```

### 7.3 权限系统扩展方案

**Step 1: 在 Base_Action 表中添加权限点**
```sql
-- 课程管理权限
INSERT INTO Base_Action (Id, ParentId, Type, Name, Url, Value, NeedAction, Icon, Sort)
VALUES
('course_module', NULL, 0, '课程管理', '/Course_Manage', NULL, 0, 'book', 100),
('course_view', 'course_module', 2, '查看课程', NULL, 'Course.View', 1, NULL, 1),
('course_add', 'course_module', 2, '新增课程', NULL, 'Course.Add', 1, NULL, 2),
('course_edit', 'course_module', 2, '编辑课程', NULL, 'Course.Edit', 1, NULL, 3),
('course_delete', 'course_module', 2, '删除课程', NULL, 'Course.Delete', 1, NULL, 4),
('course_audit', 'course_module', 2, '审核课程', NULL, 'Course.Audit', 1, NULL, 5);
```

**Step 2: 在控制器中使用权限标记**
```csharp
[Route("Course_Manage/[controller]/[action]")]
public class CourseController : BaseApiController
{
    [HttpPost]
    [ApiPermission("Course.Add")]  // 使用现有框架的权限标记
    public async Task<AjaxResult> SaveData(CourseEditInputDTO input)
    {
        // 自动权限检查，无权限返回403
    }

    [HttpPost]
    [ApiPermission("Course.Audit")]
    public async Task<AjaxResult> AuditCourse(string courseId, bool isPass)
    {
        // 只有有审核权限的角色可以调用
    }
}
```

**Step 3: 前端权限指令**
```vue
<template>
  <a-button type="primary" v-action:Course.Add @click="add">
    新增课程
  </a-button>

  <a-button danger v-action:Course.Delete @click="del">
    删除课程
  </a-button>
</template>
```

### 7.4 缓存服务扩展

**基于 BaseCache<T> 创建课程缓存：**
```csharp
public interface ICourseCache : IScopedDependency
{
    Task<CourseDTO> GetCourseCacheAsync(string courseId);
    Task RemoveCourseCacheAsync(string courseId);
}

public class CourseCache : BaseCache<CourseDTO>, ICourseCache
{
    private readonly ICourseBusiness _courseBusiness;

    public CourseCache(IDistributedCache cache, ICourseBusiness courseBusiness)
        : base(cache)
    {
        _courseBusiness = courseBusiness;
    }

    public async Task<CourseDTO> GetCourseCacheAsync(string courseId)
    {
        var cacheKey = $"Course:{courseId}";
        var cached = await GetCacheAsync(cacheKey);

        if (cached != null)
            return cached;

        // 缓存未命中，查询数据库
        var course = await _courseBusiness.GetTheDataAsync(courseId);

        // 缓存30分钟
        await SetCacheAsync(cacheKey, course, TimeSpan.FromMinutes(30));

        return course;
    }

    public async Task RemoveCourseCacheAsync(string courseId)
    {
        var cacheKey = $"Course:{courseId}";
        await RemoveCacheAsync(cacheKey);
    }
}
```

### 7.5 代码生成器使用指南

**Step 1: 在 BuildCode 模块中配置**
- 选择数据库连接
- 选择要生成的表（如：Course）
- 选择生成模板

**Step 2: 生成代码**
```
生成内容：
1. Entity（实体类）：Course.cs
2. IBusiness（业务接口）：ICourseBusiness.cs
3. Business（业务实现）：CourseBusiness.cs
4. Controller（控制器）：CourseController.cs
5. DTO（数据传输对象）：CourseDTO.cs
6. 前端List页面：Course/List.vue
7. 前端Edit表单：Course/EditForm.vue
```

**Step 3: 手动优化**
- 添加业务逻辑
- 完善表单验证
- 优化查询条件
- 添加权限标记

### 7.6 数据库迁移策略

**扩展现有表（Base_User, Base_Department）：**
```bash
# 1. 添加迁移
dotnet ef migrations add ExtendBaseUser

# 2. 查看SQL
dotnet ef migrations script

# 3. 执行迁移
dotnet ef database update
```

**创建新表（Course, Order等）：**
```bash
# 1. 创建实体类
public class Course
{
    [Key]
    public string Id { get; set; }
    // ...其他字段
}

# 2. 在 DbContext 中注册
public DbSet<Course> Course { get; set; }

# 3. 生成迁移
dotnet ef migrations add AddCourse

# 4. 更新数据库
dotnet ef database update
```

### 7.7 依赖注入标记使用

**框架提供的 DI 标记：**
```csharp
// 瞬时生命周期（每次请求创建新实例）
public class CourseBusiness : BaseBusiness<Course>, ICourseBusiness, ITransientDependency
{
}

// 作用域生命周期（同一请求内共享）
public class Operator : IOperator, IScopedDependency
{
}

// 单例生命周期（全局唯一）
public class ConfigService : IConfigService, ISingletonDependency
{
}
```

**自动注册：**
```csharp
// Startup.cs 中已配置自动扫描
services.AddFxServices();  // 自动注册所有标记了 DI 接口的服务
```

### 7.8 前端组件复用

| 现有组件 | 应用场景 |
|---------|---------|
| **CSelect** | 分类选择、讲师选择、机构选择 |
| **CUploadFile** | 课程资料上传、作业附件上传 |
| **CUploadImg** | 课程封面、轮播图上传 |
| **WangEditor** | 课程描述、文章内容、问答编辑 |
| **IconSelector** | 课程分类图标选择 |
| **GlobalHeader** | 顶部导航（复用） |
| **SideMenu** | 侧边菜单（根据权限动态生成） |
| **MultiTab** | 多标签页（复用） |
| **Exception** | 403/404/500 错误页（复用） |

### 7.9 工具类复用

| 工具类 | 方法 | 应用场景 |
|-------|------|---------|
| **TreeHelper** | ListToTree | 课程分类树、机构树 |
| **IdHelper** | GetId | 生成课程ID、订单ID等 |
| **JsonExtention** | ToJson | 对象序列化 |
| **ProcessHelper** | - | 流程处理 |
| **TypeHelper** | ChangeType | 类型转换 |
| **EnumExtension** | GetDescription | 枚举描述 |

### 7.10 集成优势总结

✅ **开发效率提升 60%**
- 代码生成器自动生成 CRUD
- 基类提供通用能力
- 组件直接复用

✅ **代码质量保证**
- 统一的架构风格
- AOP 自动日志记录
- 权限自动校验

✅ **系统稳定性**
- 成熟的框架经过验证
- 软删除保护数据
- 异常统一处理

✅ **可维护性强**
- 清晰的分层结构
- 依赖注入解耦
- 配置集中管理

---

## 八、工作量预估（基于框架优化后）

### 8.1 详细工作量分解

| 模块 | 功能点 | 后端 | 前端 | 测试 | 小计 | 框架加成 |
|-----|--------|------|------|------|------|---------|
| **基础管理** | Base_User/Department扩展 | 2 | 2 | 1 | 5 | ✅ 复用现有 |
| **课程管理** | Course/Chapter/Lesson | 8 | 8 | 3 | 19 | ✅ BuildCode生成 |
| **学习管理** | 学习记录/笔记/问答 | 5 | 5 | 2 | 12 | ✅ BuildCode生成 |
| **作业考试** | 题库/作业/考试 | 7 | 7 | 2 | 16 | ✅ BuildCode生成 |
| **评价系统** | 课程/讲师评价 | 2 | 2 | 1 | 5 | ✅ BuildCode生成 |
| **证书系统** | 模板/生成/验证 | 3 | 3 | 1 | 7 | 图片处理 |
| **消息通知** | 站内信/邮件 | 3 | 3 | 1 | 7 | ✅ SignalR |
| **数据统计** | 平台/课程/学员统计 | 4 | 4 | 1 | 9 | ✅ LINQ聚合 |
| **权限扩展** | 权限点配置 | 1 | 1 | 0.5 | 2.5 | ✅ Base_Action |
| **缓存优化** | Redis缓存 | 1 | 0 | 0.5 | 1.5 | ✅ BaseCache |
| **系统集成测试** | 全链路测试 | 2 | 2 | 4 | 8 | - |
| **总计** | - | **38** | **37** | **17** | **92人天** | - |

### 8.2 对比方案节省分析

| 项目 | 最初方案 | v2.0方案 | v3.0方案(当前) | 节省 | 原因 |
|-----|---------|---------|--------------|------|------|
| **去掉直播模块** | 240人天 | 135人天 | 92人天 | 24人天 | 需求调整 |
| **去掉支付模块** | - | 135人天 | 92人天 | 17人天 | 聚焦学习 |
| **去掉运营模块** | - | 135人天 | 92人天 | 7人天 | 简化功能 |
| **合并教师表** | - | 135人天 | 92人天 | 8人天 | 使用Base_User |
| **合并机构表** | - | 135人天 | 92人天 | 6人天 | 使用Base_Department |
| **代码生成器** | - | - | 92人天 | 40人天 | BuildCode自动生成 |
| **框架能力复用** | - | - | 92人天 | 27人天 | AOP/缓存/权限 |
| **累计节省** | 240人天 | 135人天 | **92人天** | **148人天** | **节省62%** |

### 8.3 团队配置建议

**方案一：快速交付（推荐）**
- 2名后端开发
- 2名前端开发
- 1名测试工程师
- **交付周期：** 约2个月

**方案二：稳健交付**
- 1名后端开发
- 1名前端开发
- 1名测试工程师（兼任）
- **交付周期：** 约3-4个月

### 8.4 风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|-----|------|------|---------|
| 视频转码延迟 | +2天 | 低 | 使用成熟的云服务商（阿里云/腾讯云） |
| 权限体系复杂度 | +2天 | 低 | 充分利用现有Base_Action |
| 前后端联调 | +3天 | 中 | 提前约定接口规范、使用Swagger |
| 考试防作弊 | +3天 | 中 | 前端监控 + 后端校验 |
| 性能优化 | +2天 | 中 | 预留优化时间、使用缓存 |

**风险缓冲：** 建议预留 10% 时间（约8天）用于风险应对

---

## 九、下一步行动

### 立即开始
1. 确认功能优先级
2. 细化第一阶段需求
3. 设计数据库表结构
4. 搭建项目基础（扩展现有框架）
5. 制定开发计划

### 技术选型确认
- [ ] 视频存储方案
  - 推荐：阿里云OSS + 阿里云MPS（性价比高，功能全面）
  - 备选：腾讯云COS + 腾讯云MPS
- [ ] 搜索方案
  - MVP阶段：MySQL LIKE（快速上线）
  - 优化阶段：Elasticsearch（大数据量场景，可选）
- [ ] 播放器方案
  - 推荐：阿里云播放器（与MPS集成好）
  - 备选：DPlayer（开源免费，轻量级）

### 设计工作
- [ ] 数据库详细设计（DDL脚本）
- [ ] API接口文档（Swagger）
- [ ] 前端原型设计（Axure/Figma）
- [ ] 系统架构图

---

## 附录

### A. 参考竞品
- 网易云课堂
- 腾讯课堂
- 慕课网
- 极客时间
- B站课堂

### B. 关键技术文档
- EFCore.Sharding 文档
- Ant Design Vue 组件库
- 阿里云视频点播文档
- 微信支付开发文档

### C. 代码规范
- C# 编码规范
- Vue 编码规范
- 数据库命名规范
- API接口规范

---

**文档版本：** v3.0（纯学习平台版 - 去直播/支付/运营）
**创建日期：** 2025-11-18
**更新日期：** 2025-11-18
**文档作者：** Claude Code

---

## 总结

### ✅ 优化亮点

1. **极致精简，聚焦学习**
   - ❌ 去掉直播模块（节省24人天）
   - ❌ 去掉订单支付模块（节省17人天）
   - ❌ 去掉内容运营模块（节省7人天）
   - ✅ 纯粹的学习平台，课程全部免费或通过外部系统处理支付

2. **深度集成现有框架**
   - 复用 Base_User（合并教师表）
   - 复用 Base_Department（合并机构表）
   - 充分利用 BuildCode 代码生成器
   - 利用现有 AOP、缓存、权限体系
   - 复用前端组件（上传、富文本、图标选择等）

3. **开发效率大幅提升**
   - 工作量从 **240人天** → **135人天** → **92人天**（节省62%）
   - 交付周期从 **4个月** → **2.5个月** → **2个月**
   - 数据库表从 **50张** → **43张** → **35张**（8张复用 + 27张新增）

4. **架构优势**
   - 统一的用户管理（学员、讲师、管理员）
   - 成熟的权限体系（RBAC）
   - 完善的日志审计（AOP自动记录）
   - 灵活的缓存策略（BaseCache扩展）
   - 规范的代码风格（框架约束）

### 🎯 实施路线（10周）

1. **Phase 1（3周）：** MVP核心 → 课程、学习、播放
2. **Phase 2（2周）：** 学习完善 → 笔记、问答、评价
3. **Phase 3（3周）：** 作业考试 → 题库、作业、考试、证书
4. **Phase 4（2周）：** 统计优化 → 数据统计、性能优化

**总交付：10周（约2-2.5个月）**

### 📊 核心模块（9个）

1. ✅ 基础管理 - 用户、机构、权限
2. ✅ 课程管理 - 课程、章节、课时、分类
3. ✅ 学习管理 - 学习记录、笔记、问答
4. ✅ 作业考试 - 题库、作业、试卷、考试
5. ✅ 评价系统 - 课程评价、教师评价
6. ✅ 证书系统 - 证书模板、自动生成
7. ✅ 消息通知 - 站内信、邮件
8. ✅ 数据统计 - 平台、课程、学习统计
9. ✅ 系统配置 - 字典、日志、配置

### 📌 下一步行动

1. ✅ 确认需求和优先级（已精简为纯学习平台）
2. 搭建开发环境（数据库、Redis、OSS）
3. 扩展 Base_User 和 Base_Department 表结构
4. 使用 BuildCode 生成 Course 模块代码
5. 配置权限点（Base_Action）
6. 开始第一阶段开发

---

> 💡 **核心价值：**
> - **极致精简**：去掉商业交易和运营模块，聚焦核心学习功能
> - **快速交付**：92人天，2个月即可完成核心功能
> - **易于扩展**：未来需要支付或运营功能时，可以基于现有架构快速扩展
> - **成本最低**：充分利用框架能力，开发效率提升62%

**适用场景：** 企业内训平台、高校网课系统、公益学习平台等不涉及商业交易的场景
