# 博客系统用户端开发完整教程

> 基于现有后端API和管理端架构的用户端Vue.js项目开发指南

---

## 📋 目录

- [1. 项目概述](#1-项目概述)
- [2. 现有系统分析](#2-现有系统分析)
- [3. 用户端架构设计](#3-用户端架构设计)
- [4. 技术栈选型](#4-技术栈选型)
- [5. 项目初始化](#5-项目初始化)
- [6. 目录结构设计](#6-目录结构设计)
- [7. 核心功能实现](#7-核心功能实现)
- [8. API接口对接](#8-api接口对接)
- [9. 页面开发详解](#9-页面开发详解)
- [10. 部署上线](#10-部署上线)

---

## 1. 项目概述

### 1.1 项目背景

你现有的博客管理系统采用前后端分离架构：

- **后端**: ASP.NET Core 5.0 + MySQL
- **管理端前端**: Vue 2.5 + Ant Design Vue + Vuex
- **认证方式**: JWT Token + 签名验证

现在需要开发一个**面向普通用户的博客前台系统**，用于文章浏览、评论互动、用户注册登录等功能。

### 1.2 用户端目标

用户端将实现以下核心功能：

1. **文章展示**：文章列表、详情、分类筛选、标签筛选、搜索
2. **用户系统**：注册、登录、个人中心、个人资料编辑
3. **互动功能**：文章评论、点赞、评论回复
4. **其他功能**：热门文章、推荐文章、归档、关于我们

### 1.3 与管理端的区别

| 对比项 | 管理端 | 用户端 |
|--------|--------|--------|
| **目标用户** | 管理员、作者 | 普通访客、注册用户 |
| **主要功能** | 内容管理、权限管理 | 内容浏览、互动评论 |
| **UI风格** | 后台管理风格（Ant Design） | 博客前台风格（简洁美观） |
| **权限控制** | 基于角色的复杂权限 | 简单的登录/游客权限 |
| **响应式** | 主要面向桌面 | 需完全响应式适配移动端 |

---

## 2. 现有系统分析

### 2.1 后端API架构

#### 2.1.1 API基础信息

```
API根地址: http://localhost:5000
请求方式: POST (主要)
数据格式: JSON
认证方式: JWT Bearer Token
签名验证: MD5(appId + time + guid + body + appSecret)
```

#### 2.1.2 核心API接口列表

**用户认证模块 (Base_Manage/Home)**

```javascript
// 用户登录
POST /Base_Manage/Home/SubmitLogin
Request: { userName: string, password: string }
Response: { Success: bool, Msg: string, Data: token }

// 获取当前用户信息
POST /Base_Manage/Home/GetOperatorInfo
Request: {} (需Token)
Response: { Success: bool, Data: { Id, UserName, RealName, ... } }

// 修改密码
POST /Base_Manage/Home/ChangePwd
Request: { oldPwd: string, newPwd: string }
```

**文章模块 (Blog_Manage/blog_article)**

```javascript
// 获取文章列表 (分页)
POST /Blog_Manage/blog_article/GetDataList
Request: {
  PageIndex: number,
  PageRows: number,
  SortField: string,
  SortType: "asc" | "desc",
  Search: {
    Condition?: "Title" | "Summary" | "Content",
    Keyword?: string,
    Status?: 0 | 1 | 2,  // 0=草稿, 1=已发布, 2=已隐藏
    IsTop?: 0 | 1,
    IsRecommend?: 0 | 1,
    CategoryId?: string
  }
}
Response: {
  Success: bool,
  Data: Article[],
  Total: number,
  PageIndex: number,
  PageRows: number
}

// 获取文章详情
POST /Blog_Manage/blog_article/GetTheData
Request: { id: string }
Response: {
  Success: bool,
  Data: {
    Id: string,
    Title: string,
    Summary: string,
    Content: string,  // HTML富文本
    CoverImage: string,
    CategoryId: string,
    CategoryName: string,
    AuthorId: string,
    AuthorName: string,
    Status: number,
    IsTop: number,
    IsRecommend: number,
    ViewCount: number,
    LikeCount: number,
    CommentCount: number,
    PublishTime: string,
    CreatedTime: string
  }
}
```

**分类模块 (Blog_Manage/blog_category)**

```javascript
// 获取分类列表
POST /Blog_Manage/blog_category/GetDataList
Request: {
  PageIndex: 1,
  PageRows: 100,
  Search: { Status: 1 }  // 只获取启用的分类
}
Response: {
  Success: bool,
  Data: [
    {
      Id: string,
      Name: string,
      Description: string,
      Icon: string,
      SortIndex: number,
      ArticleCount: number
    }
  ]
}
```

**评论模块 (Blog_Manage/blog_comment)**

```javascript
// 获取文章评论列表
POST /Blog_Manage/blog_comment/GetDataList
Request: {
  PageIndex: number,
  PageRows: number,
  Search: {
    ArticleId: string,
    Status: 1  // 只获取正常评论
  }
}
Response: {
  Success: bool,
  Data: [
    {
      Id: string,
      ArticleId: string,
      UserId: string,
      UserName: string,  // 需要联表查询
      Content: string,
      ParentId: string,
      ReplyToUserId: string,
      LikeCount: number,
      CreatedTime: string
    }
  ]
}

// 发表评论 (需登录)
POST /Blog_Manage/blog_comment/SaveData
Request: {
  ArticleId: string,
  Content: string,
  ParentId?: string,       // 回复评论时传入
  ReplyToUserId?: string   // @某人时传入
}
Response: { Success: bool, Msg: string }
```

**点赞模块 (Blog_Manage/blog_like)**

```javascript
// 点赞文章 (需登录)
POST /Blog_Manage/blog_like/SaveData
Request: {
  ArticleId: string
}
Response: { Success: bool, Msg: string }

// 取消点赞
POST /Blog_Manage/blog_like/DeleteData
Request: { ids: [likeId] }
```

**标签模块 (Blog_Manage/blog_tag)**

```javascript
// 获取标签列表
POST /Blog_Manage/blog_tag/GetDataList
Request: {
  PageIndex: 1,
  PageRows: 100,
  Search: { Status: 1 }
}
Response: {
  Success: bool,
  Data: [
    { Id: string, Name: string, Count: number }
  ]
}
```

### 2.2 数据模型分析

#### 文章表 (blog_article)

```typescript
interface Article {
  Id: string
  Title: string              // 文章标题
  Summary: string            // 摘要
  Content: string            // HTML富文本内容
  CoverImage: string         // 封面图URL
  CategoryId: string         // 分类ID
  CategoryName: string       // 分类名称（DTO）
  AuthorId: string           // 作者ID
  AuthorName: string         // 作者名称（DTO）
  Status: 0 | 1 | 2         // 0=草稿, 1=已发布, 2=已隐藏
  IsTop: 0 | 1              // 是否置顶
  IsRecommend: 0 | 1        // 是否推荐
  AllowComment: 0 | 1       // 是否允许评论
  ViewCount: number          // 阅读量
  LikeCount: number          // 点赞数
  CommentCount: number       // 评论数
  PublishTime: string        // 发布时间
  CreatedTime: string        // 创建时间
  UpdatedTime: string        // 更新时间
}
```

#### 用户表 (Base_User)

```typescript
interface User {
  Id: string
  UserName: string           // 用户名（唯一）
  Password: string           // 密码（加密）
  RealName: string           // 真实姓名
  Sex: 0 | 1                // 0=女, 1=男
  Birthday: string
  Avatar: string             // 头像URL
  Email: string
  Phone: string
  Introduction: string       // 个人介绍
  UserType: number          // 0=普通用户, 1=游客, 4=作者
  Status: number            // 状态
  LastLoginTime: string
  LastLoginIP: string
  CreatedTime: string
}
```

#### 评论表 (blog_comment)

```typescript
interface Comment {
  Id: string
  ArticleId: string          // 文章ID
  UserId: string             // 评论者ID
  UserName: string           // 评论者名称（DTO）
  Avatar: string             // 评论者头像（DTO）
  Content: string            // 评论内容
  ParentId: string           // 父评论ID（NULL=一级评论）
  ReplyToUserId: string      // 回复的目标用户ID
  ReplyToUserName: string    // 回复的目标用户名（DTO）
  LikeCount: number          // 点赞数
  Status: 0 | 1 | 2         // 0=已删除, 1=正常, 2=已屏蔽
  CreatedTime: string
}
```

### 2.3 管理端技术栈

分析管理端代码，技术栈如下：

```json
{
  "框架": "Vue 2.5.22",
  "路由": "Vue Router 3.0.1",
  "状态管理": "Vuex 3.1.0",
  "UI库": "Ant Design Vue 1.5.3",
  "HTTP客户端": "Axios 0.19.0",
  "富文本编辑器": "Wangeditor 3.1.1",
  "其他工具": [
    "moment.js - 日期处理",
    "md5 - 加密",
    "nprogress - 进度条"
  ]
}
```

**关键实现分析**：

1. **Axios拦截器配置** (`src/utils/plugin/axios-plugin.js`):
   - 请求拦截：添加签名验证头、JWT Token
   - 响应拦截：401跳转登录、错误统一处理

2. **Token管理** (`src/utils/cache/TokenCache.js`):
   - localStorage存储Token
   - 请求头自动携带Token

3. **路由权限守卫** (`src/permission.js`):
   - 检查Token是否存在
   - 验证路由访问权限

---

## 3. 用户端架构设计

### 3.1 整体架构图

```
┌────────────────────────────────────────────────────────┐
│                   用户端 Vue.js 应用                     │
│  ┌──────────────────────────────────────────────────┐  │
│  │                   视图层 (Views)                   │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐ │  │
│  │  │ 首页/列表  │  │ 文章详情   │  │ 个人中心   │ │  │
│  │  └────────────┘  └────────────┘  └────────────┘ │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐ │  │
│  │  │ 分类归档   │  │ 标签搜索   │  │ 关于我们   │ │  │
│  │  └────────────┘  └────────────┘  └────────────┘ │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │              组件层 (Components)                   │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐ │  │
│  │  │ 文章卡片   │  │ 评论组件   │  │ 导航栏     │ │  │
│  │  └────────────┘  └────────────┘  └────────────┘ │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐ │  │
│  │  │ 侧边栏     │  │ 底部信息   │  │ 返回顶部   │ │  │
│  │  └────────────┘  └────────────┘  └────────────┘ │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │         状态管理 (Vuex Store Modules)              │  │
│  │    user │ article │ category │ comment │ app     │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │                 API服务层 (API)                    │  │
│  │  article.js │ user.js │ comment.js │ like.js     │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │              工具层 (Utils/Plugins)                │  │
│  │  axios │ token │ storage │ formatter │ validator │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────┬───────────────────────────────────────┘
                 │ HTTP (Axios + JWT + Sign)
                 ▼
      ┌──────────────────────┐
      │  后端API (已存在)     │
      │  ASP.NET Core 5.0    │
      │  MySQL数据库         │
      └──────────────────────┘
```

### 3.2 功能模块设计

#### 3.2.1 首页模块

**功能列表**：
- 轮播图展示（置顶文章）
- 热门文章列表（按ViewCount排序）
- 推荐文章列表（IsRecommend=1）
- 最新文章列表（按PublishTime排序）
- 分类快捷入口
- 标签云

**页面路由**：`/`

#### 3.2.2 文章模块

**功能列表**：
- 文章列表（分页、筛选、搜索）
- 文章详情（富文本渲染、阅读量统计）
- 分类筛选
- 标签筛选
- 文章归档（按月份）
- 文章搜索

**页面路由**：
- `/articles` - 文章列表
- `/article/:id` - 文章详情
- `/category/:id` - 分类文章列表
- `/tag/:id` - 标签文章列表
- `/archive` - 归档页面

#### 3.2.3 用户模块

**功能列表**：
- 用户注册（需后端增加接口）
- 用户登录
- 用户信息展示
- 个人资料编辑
- 修改密码
- 我的评论
- 我的点赞

**页面路由**：
- `/login` - 登录页
- `/register` - 注册页
- `/profile` - 个人中心
- `/profile/edit` - 编辑资料
- `/profile/comments` - 我的评论
- `/profile/likes` - 我的点赞

#### 3.2.4 评论模块

**功能列表**：
- 查看评论列表（树形结构）
- 发表评论（需登录）
- 回复评论（支持嵌套）
- @用户功能
- 评论点赞
- 删除自己的评论

**组件位置**：文章详情页内的评论组件

#### 3.2.5 其他模块

**关于我们**：`/about`
**友情链接**：`/links`
**404页面**：`/404`

### 3.3 路由设计

```javascript
// src/router/index.js
const routes = [
  {
    path: '/',
    component: Layout,
    children: [
      {
        path: '',
        name: 'Home',
        component: () => import('@/views/Home/Index.vue'),
        meta: { title: '首页' }
      },
      {
        path: 'articles',
        name: 'ArticleList',
        component: () => import('@/views/Article/List.vue'),
        meta: { title: '文章列表' }
      },
      {
        path: 'article/:id',
        name: 'ArticleDetail',
        component: () => import('@/views/Article/Detail.vue'),
        meta: { title: '文章详情' }
      },
      {
        path: 'category/:id',
        name: 'CategoryArticles',
        component: () => import('@/views/Category/Articles.vue'),
        meta: { title: '分类文章' }
      },
      {
        path: 'archive',
        name: 'Archive',
        component: () => import('@/views/Archive/Index.vue'),
        meta: { title: '文章归档' }
      },
      {
        path: 'about',
        name: 'About',
        component: () => import('@/views/About/Index.vue'),
        meta: { title: '关于我们' }
      }
    ]
  },
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/User/Login.vue'),
    meta: { title: '登录' }
  },
  {
    path: '/register',
    name: 'Register',
    component: () => import('@/views/User/Register.vue'),
    meta: { title: '注册' }
  },
  {
    path: '/profile',
    component: ProfileLayout,
    meta: { requiresAuth: true },
    children: [
      {
        path: '',
        name: 'Profile',
        component: () => import('@/views/User/Profile.vue'),
        meta: { title: '个人中心' }
      },
      {
        path: 'edit',
        name: 'ProfileEdit',
        component: () => import('@/views/User/Edit.vue'),
        meta: { title: '编辑资料' }
      },
      {
        path: 'comments',
        name: 'MyComments',
        component: () => import('@/views/User/Comments.vue'),
        meta: { title: '我的评论' }
      }
    ]
  },
  {
    path: '/404',
    name: 'NotFound',
    component: () => import('@/views/Error/404.vue')
  },
  {
    path: '*',
    redirect: '/404'
  }
]
```

### 3.4 状态管理设计

#### Vuex Store模块划分

```javascript
// src/store/index.js
import Vue from 'vue'
import Vuex from 'vuex'
import app from './modules/app'
import user from './modules/user'
import article from './modules/article'
import category from './modules/category'
import comment from './modules/comment'

Vue.use(Vuex)

export default new Vuex.Store({
  modules: {
    app,
    user,
    article,
    category,
    comment
  }
})
```

#### 用户模块 (user.js)

```javascript
// src/store/modules/user.js
import { login, getUserInfo } from '@/api/user'
import { setToken, getToken, removeToken } from '@/utils/auth'

const state = {
  token: getToken(),
  userInfo: null,
  isLogin: false
}

const mutations = {
  SET_TOKEN(state, token) {
    state.token = token
    state.isLogin = !!token
  },
  SET_USER_INFO(state, userInfo) {
    state.userInfo = userInfo
  },
  LOGOUT(state) {
    state.token = ''
    state.userInfo = null
    state.isLogin = false
  }
}

const actions = {
  // 登录
  async login({ commit }, { userName, password }) {
    const res = await login({ userName, password })
    if (res.Success) {
      const token = res.Data
      commit('SET_TOKEN', token)
      setToken(token)
      return true
    } else {
      throw new Error(res.Msg)
    }
  },

  // 获取用户信息
  async getUserInfo({ commit }) {
    const res = await getUserInfo()
    if (res.Success) {
      commit('SET_USER_INFO', res.Data)
      return res.Data
    } else {
      throw new Error(res.Msg)
    }
  },

  // 登出
  logout({ commit }) {
    commit('LOGOUT')
    removeToken()
  }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
```

#### 文章模块 (article.js)

```javascript
// src/store/modules/article.js
const state = {
  currentArticle: null,
  hotArticles: [],
  recommendArticles: []
}

const mutations = {
  SET_CURRENT_ARTICLE(state, article) {
    state.currentArticle = article
  },
  SET_HOT_ARTICLES(state, articles) {
    state.hotArticles = articles
  },
  SET_RECOMMEND_ARTICLES(state, articles) {
    state.recommendArticles = articles
  }
}

const actions = {
  // 获取热门文章
  async fetchHotArticles({ commit }) {
    // API调用
  },

  // 获取推荐文章
  async fetchRecommendArticles({ commit }) {
    // API调用
  }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
```

---

## 4. 技术栈选型

### 4.1 核心技术栈

基于现有管理端的技术栈，用户端保持一致性：

```json
{
  "框架": "Vue 2.6+",
  "构建工具": "Vue CLI 4.x",
  "路由": "Vue Router 3.x",
  "状态管理": "Vuex 3.x",
  "HTTP客户端": "Axios 0.21+",
  "UI库": "Element UI 2.15+ (用户端更友好)",
  "CSS预处理": "Sass/SCSS",
  "日期处理": "moment.js 或 dayjs",
  "Markdown渲染": "marked + highlight.js",
  "加密": "md5",
  "其他": "nprogress (进度条)"
}
```

### 4.2 为什么选择Element UI而非Ant Design Vue？

| 特性 | Ant Design Vue | Element UI |
|------|----------------|------------|
| **定位** | 企业级后台 | 通用Web应用 |
| **风格** | 商务、专业 | 简洁、现代 |
| **适用场景** | 管理后台 | 前台用户界面 |
| **组件丰富度** | 非常丰富 | 丰富 |
| **移动端适配** | 一般 | 较好 |
| **文档** | 完善 | 完善 |

**推荐**：用户端使用Element UI，因为它更适合面向普通用户的博客前台。

### 4.3 其他可选库

```javascript
// 图片懒加载
import VueLazyload from 'vue-lazyload'

// 代码高亮
import 'highlight.js/styles/github.css'
import hljs from 'highlight.js'

// Markdown渲染
import marked from 'marked'

// 滚动动画
import AOS from 'aos'
import 'aos/dist/aos.css'

// 回到顶部
import BackToTop from '@/components/BackToTop'

// 图片预览
import Viewer from 'v-viewer'
import 'viewerjs/dist/viewer.css'
```

---

## 5. 项目初始化

### 5.1 使用Vue CLI创建项目

```bash
# 1. 安装Vue CLI (如果没有)
npm install -g @vue/cli

# 2. 创建项目
vue create blog-client

# 选择配置：
# ? Please pick a preset: Manually select features
# ? Check the features needed for your project:
#   ◉ Babel
#   ◉ Router
#   ◉ Vuex
#   ◉ CSS Pre-processors
#   ◉ Linter / Formatter
# ? Use history mode for router? No
# ? Pick a CSS pre-processor: Sass/SCSS (with dart-sass)
# ? Pick a linter / formatter config: ESLint + Standard config
# ? Pick additional lint features: Lint on save
# ? Where do you prefer placing config: In dedicated config files

# 3. 进入项目目录
cd blog-client

# 4. 安装依赖
npm install element-ui axios vuex-persistedstate md5 moment nprogress marked highlight.js vue-lazyload aos v-viewer --save

# 5. 安装开发依赖
npm install sass sass-loader --save-dev

# 6. 启动开发服务器
npm run serve
```

### 5.2 项目配置

#### 5.2.1 环境变量配置

创建 `.env.development` (开发环境)：

```env
# 开发环境配置
NODE_ENV=development
VUE_APP_BASE_API=http://localhost:5000
VUE_APP_APP_ID=BlogClient
VUE_APP_APP_SECRET=your_app_secret_here
VUE_APP_TIMEOUT=10000
```

创建 `.env.production` (生产环境)：

```env
# 生产环境配置
NODE_ENV=production
VUE_APP_BASE_API=https://your-api-domain.com
VUE_APP_APP_ID=BlogClient
VUE_APP_APP_SECRET=your_app_secret_here
VUE_APP_TIMEOUT=10000
```

#### 5.2.2 Vue配置文件

创建 `vue.config.js`：

```javascript
const path = require('path')

function resolve(dir) {
  return path.join(__dirname, dir)
}

module.exports = {
  publicPath: process.env.NODE_ENV === 'production' ? '/' : '/',
  outputDir: 'dist',
  assetsDir: 'static',
  lintOnSave: process.env.NODE_ENV === 'development',
  productionSourceMap: false,

  devServer: {
    port: 8080,
    open: true,
    overlay: {
      warnings: false,
      errors: true
    },
    proxy: {
      '/api': {
        target: process.env.VUE_APP_BASE_API,
        changeOrigin: true,
        pathRewrite: {
          '^/api': ''
        }
      }
    }
  },

  configureWebpack: {
    resolve: {
      alias: {
        '@': resolve('src')
      }
    }
  },

  chainWebpack(config) {
    // 设置svg-sprite-loader
    config.module
      .rule('svg')
      .exclude.add(resolve('src/icons'))
      .end()
    config.module
      .rule('icons')
      .test(/\.svg$/)
      .include.add(resolve('src/icons'))
      .end()
      .use('svg-sprite-loader')
      .loader('svg-sprite-loader')
      .options({
        symbolId: 'icon-[name]'
      })
      .end()
  }
}
```

#### 5.2.3 ESLint配置

`.eslintrc.js`：

```javascript
module.exports = {
  root: true,
  env: {
    node: true
  },
  extends: [
    'plugin:vue/essential',
    '@vue/standard'
  ],
  parserOptions: {
    parser: 'babel-eslint'
  },
  rules: {
    'no-console': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
    'space-before-function-paren': 0,
    'vue/multi-word-component-names': 0
  }
}
```

---

## 6. 目录结构设计

### 6.1 完整目录结构

```
blog-client/
├── public/                          # 静态资源
│   ├── favicon.ico
│   └── index.html
├── src/                             # 源代码
│   ├── api/                         # API接口封装
│   │   ├── article.js              # 文章API
│   │   ├── user.js                 # 用户API
│   │   ├── comment.js              # 评论API
│   │   ├── category.js             # 分类API
│   │   ├── like.js                 # 点赞API
│   │   └── tag.js                  # 标签API
│   ├── assets/                      # 资源文件
│   │   ├── images/                 # 图片
│   │   ├── styles/                 # 样式
│   │   │   ├── index.scss          # 全局样式
│   │   │   ├── variables.scss      # 变量
│   │   │   ├── mixins.scss         # 混入
│   │   │   └── reset.scss          # 重置样式
│   │   └── fonts/                  # 字体
│   ├── components/                  # 全局组件
│   │   ├── Layout/                 # 布局组件
│   │   │   ├── Header.vue          # 顶部导航
│   │   │   ├── Footer.vue          # 底部信息
│   │   │   ├── Sidebar.vue         # 侧边栏
│   │   │   └── index.vue           # 布局容器
│   │   ├── Article/                # 文章相关组件
│   │   │   ├── ArticleCard.vue     # 文章卡片
│   │   │   ├── ArticleList.vue     # 文章列表
│   │   │   └── HotArticles.vue     # 热门文章
│   │   ├── Comment/                # 评论组件
│   │   │   ├── CommentList.vue     # 评论列表
│   │   │   ├── CommentItem.vue     # 评论项
│   │   │   └── CommentForm.vue     # 评论表单
│   │   ├── Category/               # 分类组件
│   │   │   └── CategoryList.vue    # 分类列表
│   │   ├── Common/                 # 通用组件
│   │   │   ├── Pagination.vue      # 分页
│   │   │   ├── Loading.vue         # 加载中
│   │   │   ├── Empty.vue           # 空状态
│   │   │   └── BackToTop.vue       # 返回顶部
│   │   └── Tag/                    # 标签组件
│   │       └── TagCloud.vue        # 标签云
│   ├── config/                      # 配置文件
│   │   └── index.js                # 全局配置
│   ├── directives/                  # 自定义指令
│   │   └── lazyload.js             # 图片懒加载指令
│   ├── filters/                     # 全局过滤器
│   │   └── index.js                # 日期、文本格式化
│   ├── layouts/                     # 页面布局
│   │   ├── DefaultLayout.vue       # 默认布局
│   │   ├── ProfileLayout.vue       # 个人中心布局
│   │   └── BlankLayout.vue         # 空白布局
│   ├── router/                      # 路由配置
│   │   ├── index.js                # 路由主文件
│   │   └── routes.js               # 路由定义
│   ├── store/                       # Vuex状态管理
│   │   ├── modules/                # 模块
│   │   │   ├── app.js              # 应用状态
│   │   │   ├── user.js             # 用户状态
│   │   │   ├── article.js          # 文章状态
│   │   │   ├── category.js         # 分类状态
│   │   │   └── comment.js          # 评论状态
│   │   ├── getters.js              # 全局getters
│   │   └── index.js                # Store主文件
│   ├── utils/                       # 工具函数
│   │   ├── request.js              # Axios封装
│   │   ├── auth.js                 # Token管理
│   │   ├── validate.js             # 表单验证
│   │   ├── sign.js                 # 签名生成
│   │   ├── storage.js              # 本地存储
│   │   ├── date.js                 # 日期处理
│   │   └── index.js                # 通用工具
│   ├── views/                       # 页面/视图
│   │   ├── Home/                   # 首页
│   │   │   └── Index.vue
│   │   ├── Article/                # 文章页面
│   │   │   ├── List.vue            # 文章列表
│   │   │   └── Detail.vue          # 文章详情
│   │   ├── Category/               # 分类页面
│   │   │   └── Articles.vue        # 分类文章列表
│   │   ├── Archive/                # 归档页面
│   │   │   └── Index.vue
│   │   ├── User/                   # 用户页面
│   │   │   ├── Login.vue           # 登录
│   │   │   ├── Register.vue        # 注册
│   │   │   ├── Profile.vue         # 个人中心
│   │   │   ├── Edit.vue            # 编辑资料
│   │   │   └── Comments.vue        # 我的评论
│   │   ├── About/                  # 关于页面
│   │   │   └── Index.vue
│   │   └── Error/                  # 错误页面
│   │       └── 404.vue
│   ├── App.vue                      # 根组件
│   ├── main.js                      # 入口文件
│   └── permission.js                # 权限控制
├── .env.development                 # 开发环境变量
├── .env.production                  # 生产环境变量
├── .eslintrc.js                     # ESLint配置
├── .gitignore                       # Git忽略文件
├── babel.config.js                  # Babel配置
├── package.json                     # 项目依赖
├── README.md                        # 项目说明
└── vue.config.js                    # Vue配置
```

### 6.2 关键目录说明

#### 6.2.1 api目录

封装所有后端API调用，每个模块一个文件，便于维护和复用。

#### 6.2.2 components目录

存放可复用的全局组件，按功能模块分类。

#### 6.2.3 layouts目录

定义不同的页面布局模板，如：
- DefaultLayout：带导航栏、侧边栏、底部的标准布局
- ProfileLayout：个人中心专用布局
- BlankLayout：无导航的空白布局（登录、注册页）

#### 6.2.4 store目录

Vuex状态管理，模块化设计，每个业务模块独立管理状态。

#### 6.2.5 utils目录

工具函数库，包括HTTP请求、认证、验证、签名等通用功能。

---

## 7. 核心功能实现

### 7.1 Axios HTTP客户端封装

#### 7.1.1 签名生成工具

`src/utils/sign.js`：

```javascript
import md5 from 'md5'
import { v4 as uuidv4 } from 'uuid'
import moment from 'moment'

/**
 * 生成请求签名
 * @param {Object} data - 请求数据
 * @returns {Object} 签名信息
 */
export function generateSign(data) {
  const appId = process.env.VUE_APP_APP_ID
  const appSecret = process.env.VUE_APP_APP_SECRET
  const guid = uuidv4()
  const time = moment().format('YYYY-MM-DD HH:mm:ss')

  // 将请求数据转为JSON字符串
  const body = data ? JSON.stringify(data) : ''

  // 生成签名: md5(appId + time + guid + body + appSecret)
  const sign = md5(appId + time + guid + body + appSecret)

  return {
    appId,
    time,
    guid,
    sign
  }
}
```

#### 7.1.2 Token管理

`src/utils/auth.js`：

```javascript
const TOKEN_KEY = 'blog_token'

/**
 * 获取Token
 */
export function getToken() {
  return localStorage.getItem(TOKEN_KEY)
}

/**
 * 设置Token
 */
export function setToken(token) {
  return localStorage.setItem(TOKEN_KEY, token)
}

/**
 * 删除Token
 */
export function removeToken() {
  return localStorage.removeItem(TOKEN_KEY)
}

/**
 * 检查是否已登录
 */
export function isLoggedIn() {
  return !!getToken()
}
```

#### 7.1.3 Axios实例配置

`src/utils/request.js`：

```javascript
import axios from 'axios'
import { Message } from 'element-ui'
import { getToken, removeToken } from './auth'
import { generateSign } from './sign'
import router from '@/router'
import NProgress from 'nprogress'
import 'nprogress/nprogress.css'

// 创建axios实例
const service = axios.create({
  baseURL: process.env.VUE_APP_BASE_API,
  timeout: Number(process.env.VUE_APP_TIMEOUT) || 10000
})

// 请求拦截器
service.interceptors.request.use(
  config => {
    // 显示进度条
    NProgress.start()

    // 生成签名
    const signInfo = generateSign(config.data)
    config.headers['appId'] = signInfo.appId
    config.headers['time'] = signInfo.time
    config.headers['guid'] = signInfo.guid
    config.headers['sign'] = signInfo.sign

    // 添加Token
    const token = getToken()
    if (token) {
      config.headers['Authorization'] = 'Bearer ' + token
    }

    return config
  },
  error => {
    console.error('Request Error:', error)
    NProgress.done()
    return Promise.reject(error)
  }
)

// 响应拦截器
service.interceptors.response.use(
  response => {
    NProgress.done()

    const res = response.data

    // 如果Success为false，说明业务逻辑错误
    if (!res.Success) {
      Message({
        message: res.Msg || '请求失败',
        type: 'error',
        duration: 3000
      })
      return Promise.reject(new Error(res.Msg || '请求失败'))
    }

    return res
  },
  error => {
    NProgress.done()

    let message = '网络错误'

    if (error.response) {
      const { status } = error.response

      switch (status) {
        case 400:
          message = '请求参数错误'
          break
        case 401:
          message = '未授权，请重新登录'
          removeToken()
          router.push('/login')
          break
        case 403:
          message = '拒绝访问'
          break
        case 404:
          message = '请求资源不存在'
          break
        case 500:
          message = '服务器内部错误'
          break
        case 502:
          message = '网关错误'
          break
        case 503:
          message = '服务不可用'
          break
        default:
          message = `请求错误: ${status}`
      }
    } else if (error.message) {
      if (error.message.includes('timeout')) {
        message = '请求超时'
      } else if (error.message.includes('Network Error')) {
        message = '网络连接失败'
      }
    }

    Message({
      message,
      type: 'error',
      duration: 3000
    })

    return Promise.reject(error)
  }
)

export default service
```

### 7.2 API接口封装

#### 7.2.1 用户API

`src/api/user.js`：

```javascript
import request from '@/utils/request'

/**
 * 用户登录
 */
export function login(data) {
  return request({
    url: '/Base_Manage/Home/SubmitLogin',
    method: 'post',
    data
  })
}

/**
 * 用户注册（需要后端实现）
 */
export function register(data) {
  return request({
    url: '/Base_Manage/Base_User/SaveData',
    method: 'post',
    data: {
      ...data,
      UserType: 0,  // 普通用户
      Status: 1     // 启用状态
    }
  })
}

/**
 * 获取当前用户信息
 */
export function getUserInfo() {
  return request({
    url: '/Base_Manage/Home/GetOperatorInfo',
    method: 'post',
    data: {}
  })
}

/**
 * 修改密码
 */
export function changePassword(data) {
  return request({
    url: '/Base_Manage/Home/ChangePwd',
    method: 'post',
    data
  })
}

/**
 * 更新用户信息
 */
export function updateUserInfo(data) {
  return request({
    url: '/Base_Manage/Base_User/SaveData',
    method: 'post',
    data
  })
}

/**
 * 上传头像
 */
export function uploadAvatar(file) {
  const formData = new FormData()
  formData.append('file', file)

  return request({
    url: '/Base_Manage/Upload/UploadImg',
    method: 'post',
    data: formData,
    headers: {
      'Content-Type': 'multipart/form-data'
    }
  })
}
```

#### 7.2.2 文章API

`src/api/article.js`：

```javascript
import request from '@/utils/request'

/**
 * 获取文章列表
 */
export function getArticleList(params) {
  return request({
    url: '/Blog_Manage/blog_article/GetDataList',
    method: 'post',
    data: {
      PageIndex: params.pageIndex || 1,
      PageRows: params.pageRows || 10,
      SortField: params.sortField || 'PublishTime',
      SortType: params.sortType || 'desc',
      Search: {
        Condition: params.condition || '',
        Keyword: params.keyword || '',
        Status: 1,  // 只获取已发布的文章
        IsTop: params.isTop,
        IsRecommend: params.isRecommend,
        CategoryId: params.categoryId
      }
    }
  })
}

/**
 * 获取文章详情
 */
export function getArticleDetail(id) {
  return request({
    url: '/Blog_Manage/blog_article/GetTheData',
    method: 'post',
    data: { id }
  })
}

/**
 * 增加文章阅读量
 * 注意：这个功能需要后端提供专门的接口，或者在GetTheData时自动增加
 */
export function incrementViewCount(id) {
  return request({
    url: '/Blog_Manage/blog_article/IncrementViewCount',
    method: 'post',
    data: { id }
  })
}

/**
 * 获取热门文章（按阅读量排序）
 */
export function getHotArticles(limit = 5) {
  return request({
    url: '/Blog_Manage/blog_article/GetDataList',
    method: 'post',
    data: {
      PageIndex: 1,
      PageRows: limit,
      SortField: 'ViewCount',
      SortType: 'desc',
      Search: { Status: 1 }
    }
  })
}

/**
 * 获取推荐文章
 */
export function getRecommendArticles(limit = 5) {
  return request({
    url: '/Blog_Manage/blog_article/GetDataList',
    method: 'post',
    data: {
      PageIndex: 1,
      PageRows: limit,
      SortField: 'PublishTime',
      SortType: 'desc',
      Search: {
        Status: 1,
        IsRecommend: 1
      }
    }
  })
}

/**
 * 获取置顶文章
 */
export function getTopArticles() {
  return request({
    url: '/Blog_Manage/blog_article/GetDataList',
    method: 'post',
    data: {
      PageIndex: 1,
      PageRows: 3,
      SortField: 'PublishTime',
      SortType: 'desc',
      Search: {
        Status: 1,
        IsTop: 1
      }
    }
  })
}

/**
 * 搜索文章
 */
export function searchArticles(keyword, pageIndex = 1, pageRows = 10) {
  return request({
    url: '/Blog_Manage/blog_article/GetDataList',
    method: 'post',
    data: {
      PageIndex: pageIndex,
      PageRows: pageRows,
      SortField: 'PublishTime',
      SortType: 'desc',
      Search: {
        Condition: 'Title',
        Keyword: keyword,
        Status: 1
      }
    }
  })
}
```

#### 7.2.3 分类API

`src/api/category.js`：

```javascript
import request from '@/utils/request'

/**
 * 获取分类列表
 */
export function getCategoryList() {
  return request({
    url: '/Blog_Manage/blog_category/GetDataList',
    method: 'post',
    data: {
      PageIndex: 1,
      PageRows: 100,
      SortField: 'SortIndex',
      SortType: 'asc',
      Search: { Status: 1 }
    }
  })
}

/**
 * 获取分类详情
 */
export function getCategoryDetail(id) {
  return request({
    url: '/Blog_Manage/blog_category/GetTheData',
    method: 'post',
    data: { id }
  })
}
```

#### 7.2.4 评论API

`src/api/comment.js`：

```javascript
import request from '@/utils/request'

/**
 * 获取文章评论列表
 */
export function getCommentList(articleId, pageIndex = 1, pageRows = 20) {
  return request({
    url: '/Blog_Manage/blog_comment/GetDataList',
    method: 'post',
    data: {
      PageIndex: pageIndex,
      PageRows: pageRows,
      SortField: 'CreatedTime',
      SortType: 'asc',
      Search: {
        ArticleId: articleId,
        Status: 1  // 只获取正常评论
      }
    }
  })
}

/**
 * 发表评论
 */
export function addComment(data) {
  return request({
    url: '/Blog_Manage/blog_comment/SaveData',
    method: 'post',
    data: {
      ArticleId: data.articleId,
      Content: data.content,
      ParentId: data.parentId || null,
      ReplyToUserId: data.replyToUserId || null,
      Status: 1,
      LikeCount: 0
    }
  })
}

/**
 * 删除评论（只能删除自己的）
 */
export function deleteComment(id) {
  return request({
    url: '/Blog_Manage/blog_comment/DeleteData',
    method: 'post',
    data: { ids: [id] }
  })
}

/**
 * 获取用户的评论列表
 */
export function getUserComments(pageIndex = 1, pageRows = 10) {
  return request({
    url: '/Blog_Manage/blog_comment/GetDataList',
    method: 'post',
    data: {
      PageIndex: pageIndex,
      PageRows: pageRows,
      SortField: 'CreatedTime',
      SortType: 'desc',
      Search: {
        Status: 1
        // 后端需要根据当前用户ID过滤
      }
    }
  })
}
```

#### 7.2.5 点赞API

`src/api/like.js`：

```javascript
import request from '@/utils/request'

/**
 * 点赞文章
 */
export function likeArticle(articleId) {
  return request({
    url: '/Blog_Manage/blog_like/SaveData',
    method: 'post',
    data: { ArticleId: articleId }
  })
}

/**
 * 取消点赞
 */
export function unlikeArticle(likeId) {
  return request({
    url: '/Blog_Manage/blog_like/DeleteData',
    method: 'post',
    data: { ids: [likeId] }
  })
}

/**
 * 检查是否已点赞
 */
export function checkLikeStatus(articleId) {
  return request({
    url: '/Blog_Manage/blog_like/GetDataList',
    method: 'post',
    data: {
      PageIndex: 1,
      PageRows: 1,
      Search: { ArticleId: articleId }
      // 后端需要根据当前用户ID过滤
    }
  })
}

/**
 * 获取用户的点赞列表
 */
export function getUserLikes(pageIndex = 1, pageRows = 10) {
  return request({
    url: '/Blog_Manage/blog_like/GetDataList',
    method: 'post',
    data: {
      PageIndex: pageIndex,
      PageRows: pageRows,
      SortField: 'CreatedTime',
      SortType: 'desc'
      // 后端需要根据当前用户ID过滤
    }
  })
}
```

#### 7.2.6 标签API

`src/api/tag.js`：

```javascript
import request from '@/utils/request'

/**
 * 获取标签列表
 */
export function getTagList() {
  return request({
    url: '/Blog_Manage/blog_tag/GetDataList',
    method: 'post',
    data: {
      PageIndex: 1,
      PageRows: 100,
      SortField: 'Count',
      SortType: 'desc',
      Search: { Status: 1 }
    }
  })
}
```

### 7.3 路由权限控制

`src/permission.js`：

```javascript
import router from './router'
import store from './store'
import { Message } from 'element-ui'
import NProgress from 'nprogress'
import 'nprogress/nprogress.css'
import { getToken } from '@/utils/auth'

NProgress.configure({ showSpinner: false })

// 白名单：不需要登录即可访问的路由
const whiteList = [
  '/',
  '/articles',
  '/article',
  '/category',
  '/archive',
  '/about',
  '/login',
  '/register',
  '/404'
]

// 路由守卫
router.beforeEach(async (to, from, next) => {
  NProgress.start()

  // 设置页面标题
  document.title = to.meta.title ? `${to.meta.title} - 我的博客` : '我的博客'

  // 获取Token
  const hasToken = getToken()

  if (hasToken) {
    // 已登录
    if (to.path === '/login' || to.path === '/register') {
      // 如果已登录，访问登录/注册页，重定向到首页
      next({ path: '/' })
      NProgress.done()
    } else {
      // 检查是否已获取用户信息
      const hasUserInfo = store.getters.userInfo

      if (hasUserInfo) {
        next()
      } else {
        try {
          // 获取用户信息
          await store.dispatch('user/getUserInfo')
          next()
        } catch (error) {
          // 获取用户信息失败，清除Token并跳转登录
          await store.dispatch('user/logout')
          Message.error('获取用户信息失败，请重新登录')
          next(`/login?redirect=${to.path}`)
          NProgress.done()
        }
      }
    }
  } else {
    // 未登录

    // 检查是否在白名单中或路由路径是否匹配白名单前缀
    const isWhitelisted = whiteList.some(path => {
      if (path === to.path) return true
      if (to.path.startsWith(path + '/')) return true
      return false
    })

    if (isWhitelisted) {
      // 在白名单中，直接访问
      next()
    } else {
      // 不在白名单中，检查是否需要登录
      if (to.meta.requiresAuth) {
        // 需要登录，跳转到登录页
        next(`/login?redirect=${to.path}`)
        NProgress.done()
      } else {
        next()
      }
    }
  }
})

router.afterEach(() => {
  NProgress.done()
})
```

---

## 8. API接口对接

### 8.1 注意事项

#### 8.1.1 需要后端配合的地方

1. **CORS跨域配置**：后端需要允许前端域名的跨域请求

```csharp
// Startup.cs
services.AddCors(options =>
{
    options.AddPolicy("AllowClient", builder =>
    {
        builder.WithOrigins("http://localhost:8080", "https://your-domain.com")
               .AllowAnyMethod()
               .AllowAnyHeader()
               .AllowCredentials();
    });
});

app.UseCors("AllowClient");
```

2. **注册接口**：后端目前没有公开的用户注册接口，需要添加

```csharp
// Base_UserController.cs
[HttpPost]
[AllowAnonymous]
public async Task<AjaxResult> Register([FromBody] RegisterDTO input)
{
    // 验证用户名是否已存在
    // 创建用户账号
    // 返回成功消息
}
```

3. **阅读量统计**：需要专门的接口或在获取详情时自动增加

```csharp
// blog_articleController.cs
[HttpPost]
public async Task IncrementViewCount(string id)
{
    var article = await _bus.GetTheDataAsync(id);
    article.ViewCount++;
    await _bus.UpdateAsync(article);
}
```

4. **评论用户信息**：评论列表需要联表查询用户信息（用户名、头像）

```csharp
// blog_commentBusiness.cs - GetDataListAsync
var query = from comment in GetIQueryable()
            join user in Db.GetIQueryable<Base_User>() on comment.UserId equals user.Id
            select new CommentDTO
            {
                Id = comment.Id,
                Content = comment.Content,
                UserId = comment.UserId,
                UserName = user.UserName,
                Avatar = user.Avatar,
                // ...
            };
```

5. **点赞状态查询**：需要根据用户ID和文章ID查询是否已点赞

```csharp
// blog_likeBusiness.cs
public async Task<bool> HasLikedAsync(string userId, string articleId)
{
    return await GetIQueryable()
        .AnyAsync(x => x.UserId == userId && x.ArticleId == articleId);
}
```

#### 8.1.2 前端处理策略

1. **游客模式**：未登录用户可以浏览文章，但不能评论和点赞
2. **错误提示**：API调用失败时给出友好提示
3. **加载状态**：数据加载时显示骨架屏或loading
4. **空状态**：无数据时显示空状态提示

### 8.2 测试API连通性

创建一个测试页面来验证API连接：

`src/views/Test/ApiTest.vue`：

```vue
<template>
  <div class="api-test">
    <h2>API接口测试</h2>

    <el-card class="test-card">
      <h3>1. 获取文章列表</h3>
      <el-button type="primary" @click="testGetArticles">测试</el-button>
      <pre v-if="articlesResult">{{ articlesResult }}</pre>
    </el-card>

    <el-card class="test-card">
      <h3>2. 获取分类列表</h3>
      <el-button type="primary" @click="testGetCategories">测试</el-button>
      <pre v-if="categoriesResult">{{ categoriesResult }}</pre>
    </el-card>

    <el-card class="test-card">
      <h3>3. 用户登录</h3>
      <el-input v-model="loginForm.userName" placeholder="用户名" style="width: 200px; margin-right: 10px;"></el-input>
      <el-input v-model="loginForm.password" type="password" placeholder="密码" style="width: 200px; margin-right: 10px;"></el-input>
      <el-button type="primary" @click="testLogin">登录</el-button>
      <pre v-if="loginResult">{{ loginResult }}</pre>
    </el-card>
  </div>
</template>

<script>
import { getArticleList } from '@/api/article'
import { getCategoryList } from '@/api/category'
import { login } from '@/api/user'

export default {
  name: 'ApiTest',
  data() {
    return {
      articlesResult: null,
      categoriesResult: null,
      loginResult: null,
      loginForm: {
        userName: '',
        password: ''
      }
    }
  },
  methods: {
    async testGetArticles() {
      try {
        const res = await getArticleList({ pageIndex: 1, pageRows: 5 })
        this.articlesResult = JSON.stringify(res, null, 2)
        this.$message.success('获取成功')
      } catch (error) {
        this.$message.error('获取失败: ' + error.message)
      }
    },

    async testGetCategories() {
      try {
        const res = await getCategoryList()
        this.categoriesResult = JSON.stringify(res, null, 2)
        this.$message.success('获取成功')
      } catch (error) {
        this.$message.error('获取失败: ' + error.message)
      }
    },

    async testLogin() {
      try {
        const res = await login(this.loginForm)
        this.loginResult = JSON.stringify(res, null, 2)
        this.$message.success('登录成功')
      } catch (error) {
        this.$message.error('登录失败: ' + error.message)
      }
    }
  }
}
</script>

<style scoped>
.api-test {
  padding: 20px;
}

.test-card {
  margin-bottom: 20px;
}

pre {
  margin-top: 10px;
  padding: 10px;
  background: #f5f5f5;
  border-radius: 4px;
  max-height: 300px;
  overflow: auto;
}
</style>
```

---

## 9. 页面开发详解

### 9.1 首页开发

`src/views/Home/Index.vue`：

```vue
<template>
  <div class="home-page">
    <!-- 轮播图 - 置顶文章 -->
    <section class="banner-section">
      <el-carousel height="400px" indicator-position="inside">
        <el-carousel-item v-for="article in topArticles" :key="article.Id">
          <div
            class="banner-item"
            :style="{ backgroundImage: `url(${article.CoverImage})` }"
            @click="goToArticle(article.Id)">
            <div class="banner-content">
              <h2 class="banner-title">{{ article.Title }}</h2>
              <p class="banner-summary">{{ article.Summary }}</p>
              <div class="banner-meta">
                <span><i class="el-icon-view"></i> {{ article.ViewCount }}</span>
                <span><i class="el-icon-star-off"></i> {{ article.LikeCount }}</span>
                <span><i class="el-icon-chat-line-square"></i> {{ article.CommentCount }}</span>
              </div>
            </div>
          </div>
        </el-carousel-item>
      </el-carousel>
    </section>

    <!-- 主要内容区 -->
    <div class="main-container">
      <div class="content-wrapper">
        <!-- 左侧：文章列表 -->
        <div class="left-content">
          <!-- 推荐文章 -->
          <section class="article-section">
            <div class="section-header">
              <h3 class="section-title">
                <i class="el-icon-star-on"></i> 推荐文章
              </h3>
            </div>
            <article-list :articles="recommendArticles" />
          </section>

          <!-- 最新文章 -->
          <section class="article-section">
            <div class="section-header">
              <h3 class="section-title">
                <i class="el-icon-document"></i> 最新文章
              </h3>
              <router-link to="/articles" class="more-link">
                查看更多 <i class="el-icon-arrow-right"></i>
              </router-link>
            </div>
            <article-list :articles="latestArticles" />

            <!-- 分页 -->
            <div class="pagination-wrapper">
              <el-pagination
                background
                layout="prev, pager, next"
                :total="total"
                :page-size="pageSize"
                :current-page.sync="currentPage"
                @current-change="handlePageChange">
              </el-pagination>
            </div>
          </section>
        </div>

        <!-- 右侧：侧边栏 -->
        <div class="right-sidebar">
          <!-- 搜索框 -->
          <div class="search-box">
            <el-input
              v-model="searchKeyword"
              placeholder="搜索文章..."
              prefix-icon="el-icon-search"
              @keyup.enter.native="handleSearch">
              <el-button slot="append" icon="el-icon-search" @click="handleSearch"></el-button>
            </el-input>
          </div>

          <!-- 热门文章 -->
          <el-card class="sidebar-card">
            <div slot="header" class="card-header">
              <i class="el-icon-trophy"></i> 热门文章
            </div>
            <hot-articles :articles="hotArticles" />
          </el-card>

          <!-- 分类 -->
          <el-card class="sidebar-card">
            <div slot="header" class="card-header">
              <i class="el-icon-folder"></i> 文章分类
            </div>
            <category-list :categories="categories" />
          </el-card>

          <!-- 标签云 -->
          <el-card class="sidebar-card">
            <div slot="header" class="card-header">
              <i class="el-icon-collection-tag"></i> 标签云
            </div>
            <tag-cloud :tags="tags" />
          </el-card>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { getArticleList, getTopArticles, getRecommendArticles, getHotArticles } from '@/api/article'
import { getCategoryList } from '@/api/category'
import { getTagList } from '@/api/tag'
import ArticleList from '@/components/Article/ArticleList.vue'
import HotArticles from '@/components/Article/HotArticles.vue'
import CategoryList from '@/components/Category/CategoryList.vue'
import TagCloud from '@/components/Tag/TagCloud.vue'

export default {
  name: 'HomePage',
  components: {
    ArticleList,
    HotArticles,
    CategoryList,
    TagCloud
  },
  data() {
    return {
      topArticles: [],
      recommendArticles: [],
      latestArticles: [],
      hotArticles: [],
      categories: [],
      tags: [],
      searchKeyword: '',
      currentPage: 1,
      pageSize: 10,
      total: 0,
      loading: false
    }
  },
  created() {
    this.init()
  },
  methods: {
    async init() {
      await Promise.all([
        this.fetchTopArticles(),
        this.fetchRecommendArticles(),
        this.fetchLatestArticles(),
        this.fetchHotArticles(),
        this.fetchCategories(),
        this.fetchTags()
      ])
    },

    // 获取置顶文章
    async fetchTopArticles() {
      try {
        const res = await getTopArticles()
        this.topArticles = res.Data || []
      } catch (error) {
        console.error('获取置顶文章失败:', error)
      }
    },

    // 获取推荐文章
    async fetchRecommendArticles() {
      try {
        const res = await getRecommendArticles(5)
        this.recommendArticles = res.Data || []
      } catch (error) {
        console.error('获取推荐文章失败:', error)
      }
    },

    // 获取最新文章
    async fetchLatestArticles() {
      this.loading = true
      try {
        const res = await getArticleList({
          pageIndex: this.currentPage,
          pageRows: this.pageSize
        })
        this.latestArticles = res.Data || []
        this.total = res.Total || 0
      } catch (error) {
        console.error('获取最新文章失败:', error)
      } finally {
        this.loading = false
      }
    },

    // 获取热门文章
    async fetchHotArticles() {
      try {
        const res = await getHotArticles(5)
        this.hotArticles = res.Data || []
      } catch (error) {
        console.error('获取热门文章失败:', error)
      }
    },

    // 获取分类
    async fetchCategories() {
      try {
        const res = await getCategoryList()
        this.categories = res.Data || []
      } catch (error) {
        console.error('获取分类失败:', error)
      }
    },

    // 获取标签
    async fetchTags() {
      try {
        const res = await getTagList()
        this.tags = res.Data || []
      } catch (error) {
        console.error('获取标签失败:', error)
      }
    },

    // 跳转到文章详情
    goToArticle(id) {
      this.$router.push(`/article/${id}`)
    },

    // 分页变化
    handlePageChange(page) {
      this.currentPage = page
      this.fetchLatestArticles()
      // 滚动到顶部
      window.scrollTo({ top: 0, behavior: 'smooth' })
    },

    // 搜索
    handleSearch() {
      if (this.searchKeyword.trim()) {
        this.$router.push({
          path: '/articles',
          query: { keyword: this.searchKeyword }
        })
      }
    }
  }
}
</script>

<style lang="scss" scoped>
.home-page {
  min-height: 100vh;
  background: #f5f7fa;
}

// 轮播图
.banner-section {
  width: 100%;

  .banner-item {
    width: 100%;
    height: 400px;
    background-size: cover;
    background-position: center;
    position: relative;
    cursor: pointer;

    &::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(to bottom, rgba(0,0,0,0.3), rgba(0,0,0,0.7));
    }

    .banner-content {
      position: absolute;
      bottom: 60px;
      left: 50%;
      transform: translateX(-50%);
      width: 1200px;
      max-width: 90%;
      color: #fff;

      .banner-title {
        font-size: 32px;
        font-weight: bold;
        margin-bottom: 15px;
      }

      .banner-summary {
        font-size: 16px;
        margin-bottom: 15px;
        opacity: 0.9;
      }

      .banner-meta {
        display: flex;
        gap: 20px;
        font-size: 14px;

        span {
          i {
            margin-right: 5px;
          }
        }
      }
    }
  }
}

// 主要内容区
.main-container {
  max-width: 1200px;
  margin: 30px auto;
  padding: 0 15px;
}

.content-wrapper {
  display: flex;
  gap: 30px;
}

.left-content {
  flex: 1;
  min-width: 0;
}

.right-sidebar {
  width: 320px;
  flex-shrink: 0;
}

// 文章区块
.article-section {
  background: #fff;
  border-radius: 8px;
  padding: 25px;
  margin-bottom: 25px;
  box-shadow: 0 2px 12px rgba(0,0,0,0.05);

  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
    padding-bottom: 15px;
    border-bottom: 2px solid #f0f0f0;

    .section-title {
      font-size: 20px;
      font-weight: bold;
      color: #333;

      i {
        margin-right: 8px;
        color: #409eff;
      }
    }

    .more-link {
      color: #409eff;
      font-size: 14px;
      text-decoration: none;

      &:hover {
        color: #66b1ff;
      }
    }
  }
}

.pagination-wrapper {
  margin-top: 30px;
  text-align: center;
}

// 侧边栏
.search-box {
  margin-bottom: 20px;
}

.sidebar-card {
  margin-bottom: 20px;

  .card-header {
    font-weight: bold;
    font-size: 16px;

    i {
      margin-right: 6px;
      color: #409eff;
    }
  }
}

// 响应式
@media (max-width: 1024px) {
  .content-wrapper {
    flex-direction: column;
  }

  .right-sidebar {
    width: 100%;
  }
}

@media (max-width: 768px) {
  .banner-content {
    .banner-title {
      font-size: 24px;
    }

    .banner-summary {
      font-size: 14px;
    }
  }
}
</style>
```

### 9.2 文章详情页开发

`src/views/Article/Detail.vue`：

```vue
<template>
  <div class="article-detail-page">
    <div class="container">
      <div v-if="loading" class="loading-wrapper">
        <el-skeleton :rows="10" animated />
      </div>

      <div v-else-if="article" class="article-wrapper">
        <!-- 文章头部 -->
        <header class="article-header">
          <h1 class="article-title">{{ article.Title }}</h1>

          <div class="article-meta">
            <div class="meta-left">
              <el-avatar :src="article.AuthorAvatar" :size="40">
                {{ article.AuthorName ? article.AuthorName[0] : 'A' }}
              </el-avatar>
              <div class="author-info">
                <span class="author-name">{{ article.AuthorName || '佚名' }}</span>
                <span class="publish-time">{{ formatTime(article.PublishTime) }}</span>
              </div>
            </div>

            <div class="meta-right">
              <span class="meta-item">
                <i class="el-icon-view"></i> {{ article.ViewCount }}
              </span>
              <span class="meta-item">
                <i class="el-icon-star-off"></i> {{ article.LikeCount }}
              </span>
              <span class="meta-item">
                <i class="el-icon-chat-line-square"></i> {{ article.CommentCount }}
              </span>
            </div>
          </div>

          <div class="article-tags">
            <el-tag
              v-if="article.CategoryName"
              type="primary"
              size="small"
              effect="plain">
              <i class="el-icon-folder"></i> {{ article.CategoryName }}
            </el-tag>
          </div>
        </header>

        <!-- 文章封面 -->
        <div v-if="article.CoverImage" class="article-cover">
          <img :src="article.CoverImage" :alt="article.Title" />
        </div>

        <!-- 文章内容 -->
        <article class="article-content" v-html="article.Content"></article>

        <!-- 文章底部操作 -->
        <footer class="article-footer">
          <el-button
            :type="isLiked ? 'danger' : 'default'"
            :icon="isLiked ? 'el-icon-star-on' : 'el-icon-star-off'"
            @click="handleLike">
            {{ isLiked ? '已点赞' : '点赞' }} ({{ article.LikeCount }})
          </el-button>

          <el-button icon="el-icon-share" @click="handleShare">
            分享
          </el-button>
        </footer>

        <!-- 评论区 -->
        <section class="comment-section">
          <h3 class="section-title">
            <i class="el-icon-chat-dot-round"></i> 评论 ({{ comments.length }})
          </h3>

          <!-- 评论表单 -->
          <comment-form
            v-if="isLoggedIn"
            @submit="handleSubmitComment"
          />

          <div v-else class="login-tip">
            <el-alert
              title="请先登录后再评论"
              type="info"
              :closable="false"
              show-icon>
              <template slot="default">
                <router-link to="/login">立即登录</router-link>
              </template>
            </el-alert>
          </div>

          <!-- 评论列表 -->
          <comment-list
            :comments="comments"
            :article-id="articleId"
            @reply="handleReplyComment"
            @delete="handleDeleteComment"
          />
        </section>
      </div>

      <el-empty v-else description="文章不存在或已被删除"></el-empty>
    </div>

    <!-- 返回顶部 -->
    <back-to-top />
  </div>
</template>

<script>
import { getArticleDetail } from '@/api/article'
import { getCommentList, addComment, deleteComment } from '@/api/comment'
import { likeArticle, unlikeArticle, checkLikeStatus } from '@/api/like'
import { mapGetters } from 'vuex'
import { formatDateTime } from '@/utils/date'
import CommentForm from '@/components/Comment/CommentForm.vue'
import CommentList from '@/components/Comment/CommentList.vue'
import BackToTop from '@/components/Common/BackToTop.vue'

export default {
  name: 'ArticleDetail',
  components: {
    CommentForm,
    CommentList,
    BackToTop
  },
  data() {
    return {
      articleId: '',
      article: null,
      comments: [],
      loading: false,
      isLiked: false,
      likeId: null
    }
  },
  computed: {
    ...mapGetters(['isLoggedIn', 'userInfo'])
  },
  created() {
    this.articleId = this.$route.params.id
    this.init()
  },
  methods: {
    async init() {
      await Promise.all([
        this.fetchArticleDetail(),
        this.fetchComments(),
        this.checkLike()
      ])
    },

    // 获取文章详情
    async fetchArticleDetail() {
      this.loading = true
      try {
        const res = await getArticleDetail(this.articleId)
        this.article = res.Data

        // TODO: 调用阅读量增加接口
        // await incrementViewCount(this.articleId)
      } catch (error) {
        this.$message.error('获取文章详情失败')
        console.error(error)
      } finally {
        this.loading = false
      }
    },

    // 获取评论列表
    async fetchComments() {
      try {
        const res = await getCommentList(this.articleId)
        this.comments = res.Data || []
      } catch (error) {
        console.error('获取评论失败:', error)
      }
    },

    // 检查点赞状态
    async checkLike() {
      if (!this.isLoggedIn) return

      try {
        const res = await checkLikeStatus(this.articleId)
        if (res.Data && res.Data.length > 0) {
          this.isLiked = true
          this.likeId = res.Data[0].Id
        }
      } catch (error) {
        console.error('检查点赞状态失败:', error)
      }
    },

    // 点赞/取消点赞
    async handleLike() {
      if (!this.isLoggedIn) {
        this.$message.warning('请先登录')
        this.$router.push('/login')
        return
      }

      try {
        if (this.isLiked) {
          // 取消点赞
          await unlikeArticle(this.likeId)
          this.isLiked = false
          this.article.LikeCount--
          this.$message.success('已取消点赞')
        } else {
          // 点赞
          const res = await likeArticle(this.articleId)
          this.isLiked = true
          this.likeId = res.Data.Id
          this.article.LikeCount++
          this.$message.success('点赞成功')
        }
      } catch (error) {
        this.$message.error('操作失败')
      }
    },

    // 分享
    handleShare() {
      const url = window.location.href
      navigator.clipboard.writeText(url).then(() => {
        this.$message.success('链接已复制到剪贴板')
      }).catch(() => {
        this.$message.error('复制失败')
      })
    },

    // 提交评论
    async handleSubmitComment(content) {
      try {
        await addComment({
          articleId: this.articleId,
          content
        })
        this.$message.success('评论成功')
        // 刷新评论列表
        await this.fetchComments()
        this.article.CommentCount++
      } catch (error) {
        this.$message.error('评论失败')
      }
    },

    // 回复评论
    async handleReplyComment({ parentId, replyToUserId, content }) {
      try {
        await addComment({
          articleId: this.articleId,
          content,
          parentId,
          replyToUserId
        })
        this.$message.success('回复成功')
        await this.fetchComments()
      } catch (error) {
        this.$message.error('回复失败')
      }
    },

    // 删除评论
    async handleDeleteComment(commentId) {
      try {
        await this.$confirm('确定删除该评论吗?', '提示', {
          confirmButtonText: '确定',
          cancelButtonText: '取消',
          type: 'warning'
        })

        await deleteComment(commentId)
        this.$message.success('删除成功')
        await this.fetchComments()
        this.article.CommentCount--
      } catch (error) {
        if (error !== 'cancel') {
          this.$message.error('删除失败')
        }
      }
    },

    // 格式化时间
    formatTime(time) {
      return formatDateTime(time)
    }
  }
}
</script>

<style lang="scss" scoped>
.article-detail-page {
  min-height: 100vh;
  background: #f5f7fa;
  padding: 30px 0;
}

.container {
  max-width: 900px;
  margin: 0 auto;
  padding: 0 15px;
}

.article-wrapper {
  background: #fff;
  border-radius: 8px;
  padding: 40px;
  box-shadow: 0 2px 12px rgba(0,0,0,0.05);
}

// 文章头部
.article-header {
  .article-title {
    font-size: 32px;
    font-weight: bold;
    color: #333;
    margin-bottom: 20px;
    line-height: 1.4;
  }

  .article-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 15px;
    padding-bottom: 15px;
    border-bottom: 1px solid #f0f0f0;

    .meta-left {
      display: flex;
      align-items: center;
      gap: 12px;

      .author-info {
        display: flex;
        flex-direction: column;
        gap: 4px;

        .author-name {
          font-weight: 500;
          color: #333;
        }

        .publish-time {
          font-size: 12px;
          color: #999;
        }
      }
    }

    .meta-right {
      display: flex;
      gap: 15px;

      .meta-item {
        color: #999;
        font-size: 14px;

        i {
          margin-right: 4px;
        }
      }
    }
  }

  .article-tags {
    margin-bottom: 20px;
  }
}

// 文章封面
.article-cover {
  width: 100%;
  margin-bottom: 30px;
  border-radius: 8px;
  overflow: hidden;

  img {
    width: 100%;
    height: auto;
    display: block;
  }
}

// 文章内容
.article-content {
  font-size: 16px;
  line-height: 1.8;
  color: #333;
  margin-bottom: 40px;

  // 富文本样式
  ::v-deep {
    img {
      max-width: 100%;
      height: auto;
      display: block;
      margin: 20px auto;
      border-radius: 4px;
    }

    p {
      margin-bottom: 15px;
    }

    h1, h2, h3, h4, h5, h6 {
      margin: 25px 0 15px;
      font-weight: bold;
    }

    code {
      background: #f5f5f5;
      padding: 2px 6px;
      border-radius: 3px;
      font-family: 'Courier New', monospace;
    }

    pre {
      background: #f5f5f5;
      padding: 15px;
      border-radius: 4px;
      overflow-x: auto;

      code {
        background: none;
        padding: 0;
      }
    }

    blockquote {
      border-left: 4px solid #409eff;
      padding-left: 15px;
      margin: 20px 0;
      color: #666;
    }
  }
}

// 文章底部
.article-footer {
  text-align: center;
  padding: 30px 0;
  border-top: 1px solid #f0f0f0;
  border-bottom: 1px solid #f0f0f0;
  margin-bottom: 40px;
}

// 评论区
.comment-section {
  .section-title {
    font-size: 20px;
    font-weight: bold;
    margin-bottom: 20px;
    color: #333;

    i {
      margin-right: 8px;
      color: #409eff;
    }
  }

  .login-tip {
    margin-bottom: 20px;

    a {
      color: #409eff;
      text-decoration: none;
      margin-left: 8px;

      &:hover {
        text-decoration: underline;
      }
    }
  }
}

// 响应式
@media (max-width: 768px) {
  .article-wrapper {
    padding: 20px;
  }

  .article-header {
    .article-title {
      font-size: 24px;
    }

    .article-meta {
      flex-direction: column;
      align-items: flex-start;
      gap: 15px;
    }
  }

  .article-content {
    font-size: 15px;
  }
}
</style>
```

### 9.3 评论组件开发

`src/components/Comment/CommentList.vue`：

```vue
<template>
  <div class="comment-list">
    <div v-if="comments.length === 0" class="empty-comments">
      <el-empty description="暂无评论，快来发表第一条评论吧！"></el-empty>
    </div>

    <div v-else class="comment-items">
      <comment-item
        v-for="comment in rootComments"
        :key="comment.Id"
        :comment="comment"
        :replies="getReplies(comment.Id)"
        :article-id="articleId"
        @reply="$emit('reply', $event)"
        @delete="$emit('delete', $event)"
      />
    </div>
  </div>
</template>

<script>
import CommentItem from './CommentItem.vue'

export default {
  name: 'CommentList',
  components: {
    CommentItem
  },
  props: {
    comments: {
      type: Array,
      default: () => []
    },
    articleId: {
      type: String,
      required: true
    }
  },
  computed: {
    // 一级评论（根评论）
    rootComments() {
      return this.comments.filter(c => !c.ParentId)
    }
  },
  methods: {
    // 获取某条评论的所有回复
    getReplies(parentId) {
      return this.comments.filter(c => c.ParentId === parentId)
    }
  }
}
</script>
```

`src/components/Comment/CommentItem.vue`：

```vue
<template>
  <div class="comment-item">
    <div class="comment-main">
      <el-avatar :src="comment.Avatar" :size="40" class="comment-avatar">
        {{ comment.UserName ? comment.UserName[0] : 'U' }}
      </el-avatar>

      <div class="comment-content">
        <div class="comment-header">
          <span class="comment-author">{{ comment.UserName }}</span>
          <span class="comment-time">{{ formatTime(comment.CreatedTime) }}</span>
        </div>

        <div class="comment-text">
          <span v-if="comment.ReplyToUserName" class="reply-to">
            回复 @{{ comment.ReplyToUserName }}:
          </span>
          {{ comment.Content }}
        </div>

        <div class="comment-actions">
          <el-button type="text" size="small" @click="showReplyForm = !showReplyForm">
            <i class="el-icon-chat-line-square"></i> 回复
          </el-button>

          <el-button
            v-if="canDelete"
            type="text"
            size="small"
            @click="handleDelete">
            <i class="el-icon-delete"></i> 删除
          </el-button>
        </div>

        <!-- 回复表单 -->
        <div v-if="showReplyForm" class="reply-form">
          <el-input
            v-model="replyContent"
            type="textarea"
            :rows="3"
            placeholder="写下你的回复..."
            maxlength="500"
            show-word-limit
          ></el-input>
          <div class="reply-form-actions">
            <el-button size="small" @click="showReplyForm = false">取消</el-button>
            <el-button
              type="primary"
              size="small"
              :loading="submitting"
              @click="handleReply">
              提交
            </el-button>
          </div>
        </div>

        <!-- 子评论（回复） -->
        <div v-if="replies && replies.length > 0" class="comment-replies">
          <comment-item
            v-for="reply in replies"
            :key="reply.Id"
            :comment="reply"
            :article-id="articleId"
            @reply="$emit('reply', $event)"
            @delete="$emit('delete', $event)"
          />
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { mapGetters } from 'vuex'
import { formatDateTime } from '@/utils/date'

export default {
  name: 'CommentItem',
  props: {
    comment: {
      type: Object,
      required: true
    },
    replies: {
      type: Array,
      default: () => []
    },
    articleId: {
      type: String,
      required: true
    }
  },
  data() {
    return {
      showReplyForm: false,
      replyContent: '',
      submitting: false
    }
  },
  computed: {
    ...mapGetters(['userInfo']),

    // 是否可以删除（只能删除自己的评论）
    canDelete() {
      return this.userInfo && this.userInfo.Id === this.comment.UserId
    }
  },
  methods: {
    // 回复
    async handleReply() {
      if (!this.replyContent.trim()) {
        this.$message.warning('请输入回复内容')
        return
      }

      this.submitting = true
      try {
        await this.$emit('reply', {
          parentId: this.comment.Id,
          replyToUserId: this.comment.UserId,
          content: this.replyContent
        })

        this.replyContent = ''
        this.showReplyForm = false
      } finally {
        this.submitting = false
      }
    },

    // 删除
    handleDelete() {
      this.$emit('delete', this.comment.Id)
    },

    // 格式化时间
    formatTime(time) {
      return formatDateTime(time)
    }
  }
}
</script>

<style lang="scss" scoped>
.comment-item {
  margin-bottom: 20px;

  .comment-main {
    display: flex;
    gap: 12px;
  }

  .comment-avatar {
    flex-shrink: 0;
  }

  .comment-content {
    flex: 1;
    min-width: 0;
  }

  .comment-header {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 8px;

    .comment-author {
      font-weight: 500;
      color: #333;
    }

    .comment-time {
      font-size: 12px;
      color: #999;
    }
  }

  .comment-text {
    color: #666;
    line-height: 1.6;
    margin-bottom: 10px;
    word-break: break-word;

    .reply-to {
      color: #409eff;
      margin-right: 4px;
    }
  }

  .comment-actions {
    display: flex;
    gap: 5px;
  }

  .reply-form {
    margin-top: 15px;
    padding: 15px;
    background: #f9f9f9;
    border-radius: 4px;

    .reply-form-actions {
      margin-top: 10px;
      text-align: right;
    }
  }

  .comment-replies {
    margin-top: 15px;
    padding-left: 20px;
    border-left: 2px solid #f0f0f0;
  }
}
</style>
```

`src/components/Comment/CommentForm.vue`：

```vue
<template>
  <div class="comment-form">
    <el-input
      v-model="content"
      type="textarea"
      :rows="4"
      placeholder="写下你的评论..."
      maxlength="500"
      show-word-limit
    ></el-input>

    <div class="form-actions">
      <el-button
        type="primary"
        :loading="submitting"
        @click="handleSubmit">
        发表评论
      </el-button>
    </div>
  </div>
</template>

<script>
export default {
  name: 'CommentForm',
  data() {
    return {
      content: '',
      submitting: false
    }
  },
  methods: {
    async handleSubmit() {
      if (!this.content.trim()) {
        this.$message.warning('请输入评论内容')
        return
      }

      this.submitting = true
      try {
        await this.$emit('submit', this.content)
        this.content = ''
      } finally {
        this.submitting = false
      }
    }
  }
}
</script>

<style lang="scss" scoped>
.comment-form {
  margin-bottom: 30px;

  .form-actions {
    margin-top: 15px;
    text-align: right;
  }
}
</style>
```

---

## 10. 部署上线

### 10.1 构建生产版本

```bash
# 1. 构建项目
npm run build

# 构建完成后会生成 dist 目录
```

### 10.2 Nginx配置

创建 `nginx.conf`：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/blog-client/dist;
    index index.html;

    # Gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Vue Router History模式配置
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API代理（可选）
    location /api {
        proxy_pass http://your-backend-api:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 10.3 Docker部署（可选）

创建 `Dockerfile`：

```dockerfile
# 构建阶段
FROM node:14-alpine as builder

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

# 生产阶段
FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

创建 `docker-compose.yml`：

```yaml
version: '3.8'

services:
  blog-client:
    build: .
    ports:
      - "80:80"
    restart: always
```

部署：

```bash
docker-compose up -d
```

---

## 总结

本教程详细介绍了基于你现有博客管理系统的用户端开发全过程，包括：

1. **项目分析**：深入分析了后端API和管理端代码
2. **架构设计**：设计了用户端的整体架构和功能模块
3. **技术选型**：选择了适合的技术栈
4. **核心实现**：实现了HTTP客户端、API封装、路由权限等核心功能
5. **页面开发**：详细讲解了首页、文章详情、评论等主要页面
6. **部署上线**：提供了生产环境的部署方案

### 后续优化建议

1. **SEO优化**：使用Nuxt.js进行服务端渲染
2. **性能优化**：图片懒加载、路由懒加载、代码分割
3. **用户体验**：添加骨架屏、加载动画、错误边界
4. **功能扩展**：文章搜索、标签筛选、用户关注、消息通知
5. **移动端**：优化移动端体验，考虑使用Vant UI
6. **国际化**：支持多语言（vue-i18n）
7. **主题切换**：支持深色模式
8. **数据统计**：接入Google Analytics或百度统计

祝你开发顺利！
